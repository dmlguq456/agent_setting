# Shared Fragment: Authorized Memory Closing

> Referenced by units that retain recurring findings. Replaces the near-identical
> closing paragraph previously restated across ~20 mode files.

Retain recurring, durable observations — the unit's own retention target (project
conventions, recurring failure patterns, domain traps) — only through the authorized
memory flow of the acting surface. Do not write memory through any other channel, and do
not store one-off task detail that the artifact already records.
