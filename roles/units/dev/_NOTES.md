# dev family — authoring residue (review required)

Merged sources per unit: `roles/modes/dev/<mode>.md` (EN), `adapters/claude/agent-modes/dev/<mode>.md` (KO), `adapters/claude/agents/dev-team.md` (team). Items below are content that found no clean single home, generalizations that dropped concrete examples, or divergences resolved by rule.

1. **Team routing table re-homes to entry.** `dev-team.md:22-30` (mode-selection triggers) is router logic; per the spec, routing lives in the entry skill only. The per-mode trigger text should inform the entry recipes for the dev units; only refactor's "default when invoked by code-execute inside autopilot-code" trigger was load-bearing enough to keep in the unit body (`dev/refactor.md`). The ambiguity rule ("ask one concise question with the recommended mode", `dev-team.md:31`) is entry-owned and not restated in any unit.

2. **Language Rule dropped as surface-owned.** `dev-team.md:15-18` defers to `roles/response-policy.md`; units inherit that via the acting surface, so it is not restated. "Keep code identifiers, file paths, and technical terms in their established technical form" (`dev-team.md:18`) has no unit home — confirm it is covered by response-policy or add it there.

3. **Model-role recommendation split for new-lib.** `dev-team.md:52-56`: refactor/backend/frontend = fast implementer; new-lib = fast implementer for simple functions, deep maker for complex API/library design; "The caller may override the model." Units declare `role: fast implementer`; the new-lib escalation is noted in its body as routing-surface-owned. The adapter's concrete model value (`dev-team.md:5`, `:53`) is dropped per the model-literal guard — concrete resolution stays in per-adapter models.conf.

4. **Team frontmatter runtime config dropped.** `dev-team.md:1-11`: `tools:` list, adapter-configured model, `color: green`, `memory: project`, `metadata.modes/blurb` are runtime-surface/node-owned configuration, not unit semantics. Node definitions must carry equivalent tool grants when the units are wired.

5. **KO stack examples generalized in EN canon (kept EN, examples recorded here).** backend KO `:11` names zod/valibot as validation libs and `:30` names TypeScript types/Prisma schema; frontend KO `:11` Next.js App Router, `:26` tokens.css/tailwind config/shadcn theme. EN modes had already generalized these ("project's validation library", "project-native routing conventions", "live design token contract") — units follow the EN portable phrasing; the concrete examples survive only for frontend token-contract wording ("token stylesheet, framework theme config, or component-library theme") and new-lib docstring styles (Google/NumPy/JSDoc, new-lib KO `:11`, kept as parenthetical).

6. **frontend visual-verification tool name.** KO `:27,:37` names `preview_screenshot`; EN `roles/modes/dev/frontend.md:24` says "adapter visual harness". Unit keeps both as a tool/adapter note (harness-neutral rule, Claude-adapter tool named as the example) per the authoring instruction. Whether `preview_screenshot` deserves a `tools:` contract ref (like the design-family harness) is open.

7. **Spec-Backed Project Check and Cross-Project User Profiles restated 4x.** `dev-team.md:33-39` and `:41-49` are load-bearing team blocks that apply to every dev unit; each unit carries a condensed version (full mode-concern examples kept only in new-lib, where the team file's examples map naturally). Candidate for a future `_shared/` or family-level fragment if the guard flags the repetition. Full command form recorded here: `python3 <agent-home>/tools/memory/mem.py profile {07_coding_convention|05_domain_expertise|04_analysis_methodology}`; precedence: project-local `<artifact-root>/analysis_project/code/experiment_conventions.md` > profiles; current-turn user instruction > all; updates flow through `/analyze-user` or `/post-it --scope user` (`dev-team.md:49`).

8. **Step-log format asymmetry.** The detailed old/new/Decision step-log format lives only in refactor (`roles/modes/dev/refactor.md:31-42`); backend/frontend/new-lib EN modes say only "write the step log". Units preserve this asymmetry faithfully. Review whether the format should generalize to all dev pipeline branches or whether code-execute owns it.

9. **refactor KO/EN Core Rules divergence resolved to the union.** KO `adapters/claude/agent-modes/dev/refactor.md:14` delegates shared safety rules to the router Common Rules (`dev-team.md:60-66`) as single source; EN restates them inline. With the team router retired, the unit re-inlines the full union (small steps, I/O preservation, signature safety, forbidden zones) — stricter/self-contained wins. Team Common Rule #1 ("one mode per invocation") is rendered as "one behavior atom per dispatch" in each unit.

10. **new-lib dependency-approval divergence resolved to stricter EN.** KO `:15` "외부 패키지 추가 시 사용자에 한 줄 확인" (one-line heads-up) vs EN `roles/modes/dev/new-lib.md:27` "explicit approval or a parent plan that already authorizes them" — unit takes the stricter EN contract.

11. **Team Agent Memory paragraph merged into memory-flow refs.** `dev-team.md:67-69` ("do not record transient invocation frequency or task state") is semantically covered by `_shared/memory-flow.md` ("do not store one-off task detail"); the specific anti-target "invocation frequency" is recorded here in case the fragment wording is judged not to cover it.
