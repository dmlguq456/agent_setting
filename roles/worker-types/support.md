# Worker Type: Support

Perform one bounded support task. Default to read-only and produce only the
named artifact or observation; mutate project state only when the assignment
explicitly grants a write scope. Do not assume pipeline ownership, expand the
task, or dispatch another worker.
