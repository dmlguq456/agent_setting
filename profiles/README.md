# Dispatch Profiles

`profiles/` catalogs **dispatch profiles** — declarations that build a masked,
partial-projection config home for a single headless dispatch. A profile
selects a harness, a model (portable role or concrete), one `worker_type`, a
specialization fragment, and an exposed subset of skills/agents/triggers. The home is generated by
`tools/profile/build-home.py` from a single repo source via symlink
partial-projection — there is no content fork.

## Catalog

| Profile | Description |
|---|---|
| `lab-runner` | Specialized experiment runner for autonomous `autopilot-lab` segments |
| `code-plan` | `code-plan` stage worker for `autopilot-code` Step 2 depth-2 dispatch (planning role) |
| `code-execute` | `code-execute` stage worker for `autopilot-code` Step 3 depth-2 dispatch (implementation role; the only source-mutating stage) |
| `code-test` | `code-test` stage worker for `autopilot-code` Step 4 depth-2 dispatch (QA test role; source read-only) |
| `code-report` | `code-report` stage worker for `autopilot-code` Step 5 depth-2 dispatch (QA fast-writer role) |

## The typed worker instruction model

See `spec/dispatch-profiles/prd.md` §2 for the full rationale. Summary:

| Layer | Content | Exposure |
|---|---|---|
| **Worker kernel** | `roles/worker-bootstrap.md`: immutable assignment, safety/scope, file-only evidence, three-line handoff | Injected by the dispatcher for every worker |
| **Worker type** | Exactly one of `roles/worker-types/{owner,stage,review,support}.md` | Explicit `worker_type` or deterministic depth/role fallback |
| **Runtime attach layer** | Small `bootstrap-<harness>.md` plus guard/settings projection | Profile only; it does not request the four main core documents |
| **Specialization** | `profiles/fragments/<name>.md` and selected Skills/agents | That profile only — masked out of main and other profiles |
| **Main orchestration** | Entry confirmation, memory lifecycle, routing/fleet integration, merge/push/cleanup, user response | Main only — never added by the profile |

Profile masking hides harness-controlled *body*, not *existence* — main can always route via this
README's name + one-line description, same contract as the skill catalog.
Masking is not inaccessibility — fragments are ordinary repo files main can
Read on demand; only the automatic-injection scope differs per profile. A
runtime may still auto-inherit project/user instruction files; that residual
input is reported separately and is not called physical profile masking.

## `profiles/` is a scoped exception to the portable-boundary rule

`profiles/` sits at top level like `roles/`, but unlike `roles/` it is a
**harness-scoped declaration directory** — every `profiles/<name>.yaml` binds
a `harness:` field, and concrete model names are permitted *inside that
binding*. This is the **intended exception** to the canonical rule in
`core/ADAPTATION.md` ("Concrete model names belong in adapter documents or
generated native files") — the rule still holds for `roles/` (portable,
harness-neutral), but a dispatch profile declaration is itself a
harness-bound adapter-facing artifact, so a concrete `model:` field there is
the rule's exception surface, not a violation of it. Prefer `model_role`
(portable) unless a concrete model is required (e.g. an opencode
glm/deepseek provider model) — see the individual `<name>.yaml` files.

## Term disambiguation — not the same "profile"

The **dispatch profile** described here (`profiles/<name>.yaml`, the
`--profile <name>` dispatch-wrapper flag, and the `profile=<name>` jobs.log
`pipe` field — together a masked per-dispatch config home) is a **different
concept** from the family's existing **mem-profile** (`mem profile <stem>`,
DB `type=profile` records, user personalization; see `core/MEMORY.md` §7.6).
Both are called "profile" in their own context — do not conflate them in
docs, grep, or recall.
