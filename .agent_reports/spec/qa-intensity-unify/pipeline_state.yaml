project_name: qa-intensity-unify
component: qa-intensity-unify     # --qa 독립 축 폐지·intensity 완전 파생 일원화 (spec/stage-dispatch·harness-layer-sync 와 독립)
created: 2026-07-10
mode: [library]                   # 하네스 계약 문서 개정 — 파이프 옵션 축
scope_note: "사용자-facing --qa 폐지, 검증 rigor = CONVENTIONS §1:71 intensity 파생 테이블 단일 경로(SoT 승격). 검증 프로세스 제거 없음(decision_74ca88 계승). adversarial 이중 정의 해소 — external adversary 요구를 intensity=adversarial 로 바인딩 이전."
inputs:
  - 사용자 결정 2026-07-10: "qa 가 따로 있어야 하나 — intensity 에 자연스럽게 따라가는 게 맞다. 전체적으로 손봐서 다음 사이클로."
  - 선행 결정 DB decision_74ca88 (계승·진화): 검증 프로세스 유지·intensity=shape·qa=verify override — 본 spec 은 잔존 override 축 폐지
  - 현행 실측 census 2026-07-10: CONVENTIONS §1:48·71·75-76·105 / 표면 분포 core 4·skills 46·wrapper 3종·jobs.log qa= 필드·fleet check:<qa>
phases:
  spec: done              # PRD v1 — QA-1~3 채택 + QA-OPEN-1(독립 override 반례 census 판정)
  scaffolding: n/a        # 계약 문서 개정 — 코드 skeleton 없음 (wrapper 파생 로직은 증분)
  design: n/a
  dev: pending            # autopilot-code 다음 사이클 — sd15-adapter-parity 수확 후 착수 (wrapper·adapter 문서 파일 겹침 회피)
last_updated: 2026-07-10  # v1 초안
decisions_locked:
  - QA-1: 사용자-facing --qa 폐지, rigor = intensity 파생 매핑(§1:71) 단일 경로·SoT 승격. 검증 프로세스 제거 없음(74ca88 계승)
  - QA-2: high-stakes 신호 자동 상향 = intensity 상향으로 재매핑(§0(B) 개정). adversarial qa-tier 제거 — external adversary 요구·검증가능성 게이트는 intensity=adversarial 바인딩 이전. analyze-user 고정도 intensity 표현
  - QA-3: 표면 정리 — SKILL argument-hint·CONVENTIONS §1·WORKFLOW §1.1·wrapper 3종(--qa optional/derived, jobs.log qa= 필드는 파생값으로 유지)·bootstrap 3종·drill fixture. fleet 라벨은 범위 외(handoff)
decisions_open:
  - QA-OPEN-1: shape 독립 rigor override 의 실존 반례(문서 파이프 fact-check 등) — 구현 사이클 census 로 판정, 있으면 그 자리 한정 내부 전용 파라미터, 없으면 완전 폐지
