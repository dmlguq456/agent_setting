project_name: skill-design-refactor
component: skill-design-refactor   # 별도 컴포넌트 (repo spec/prd.md = Unified Memory System, 무관)
created: 2026-07-13
mode: [library]
scope_note: "v6는 read-only/semantic precedence 뒤 matching entry-router를 primary로 선호하고, explicit analysis+usable artifact 부재를 analyze-project 기본 진입으로 고정한다. direct는 entry 우회가 아니며 runtime implicit matching은 model-mediated로 정직하게 보고한다."
inputs:
  - analysis_project/code/skill_design_audit.md           # 28스킬×4축 verdict·Step7 P1~P8·§6 Cluster 매핑
  - analysis_project/code/skill_design_audit_per_skill.md  # file:line 근거 (Plan Resolution 복제·flip 검증 대상)
  - research/skill-design-principles/00_briefing.md        # Predictability root virtue·Top-3 insight(hook 우회 근거)
  - research/skill-design-principles/02_standards.md       # 정량 규범 표·강제 수위(soft/hard)
  - research/skill-design-principles/04_technical_deep_dive.md  # 4축 메커니즘(attention budget 근거)
  - _internal/skill_design_audit/scan.sh (audit T3, 기존)   # 정량 규범 전수 스캔 — lint 승격 재사용
phases:
  spec: done              # PRD v6 — entry-router preference + analyze-project initial-analysis default
  scaffolding: n/a        # 기존 skills/ 리팩터 — 신규 scaffold 없음
  design: n/a             # instruction 파일 리팩터 — 시각 UI 없음
  dev: in_progress        # core/manifest/bootstrap/projection/routing fixture update
  verification: pending   # deterministic checks pending; live drill intentionally not auto-run
spec_version: v6
result: PARTIAL
back_jump:
  - from: dev
    to: spec
    date: 2026-07-13
    reason: "BLOCKING_FINDING — audit/PRD v1 이 legacy root skills/ 를 대상으로 삼았으나 live Claude source 는 adapters/claude/skills/*/SKILL.md. main 기준 두 트리 동일 확인돼 audit 진단 재사용, 대상 경로만 정정 + 미러 불변식(SD-11) 추가."
    done_before_halt: "CORE(CORE-1~4)+Cluster2(C2-1~5) root skills/ 편집 완료·커밋 cd48b25 — 재개 시 adapters 트리로 미러 적용이 첫 항목"
last_updated: 2026-07-22
decisions_locked:
  - SD-1: 실행 순서 Cluster 2(SoT) → 3(sprawl) → 1(invocation 검증 후)
  - SD-2: Plan Resolution SoT = autopilot-code 단일 authority, 5개 pointer (grep "keep in sync"=0)
  - SD-3: 설계 계약 = 정량 규범 CONVENTIONS / 4축 tenet DESIGN_PRINCIPLES 분할 + 상호 포인터
  - SD-4: 정량 규범 강제 = check.sh 구조+invocation registry gate + g7 회귀 케이스
  - SD-5 (runtime amendment): manual-only만 disable=true; parent/pipeline/preload 대상은 model-invoked 유지. 현재 13개는 parent-invoked
  - SD-6: 각 Cluster 완료 시 sync-skills + adapter doctor/parity mirror 검증 의무
  - SD-7: 원칙-하네스 충돌 3건(auto-activation hook 우회·한국어 blurb·hook 라우팅) 유지
  - SD-8: references/ 추출 시 1-depth 불변(scan.sh 재확인), autopilot-design 우선
  - SD-9: 다운스트림 = /autopilot-code --mode refactor (plans/ 누적), PRD 버전 = _internal/versions/v{N}/
  - SD-10: 진단 §5 강점 4종 회귀 금지 — refactor 후 audit rubric 재적용(drill)
  - SD-11 (v3 supersedes v2): 의미 정본 = core/capabilities/roles; Claude/Codex/OpenCode는 동급 sibling realization; root skills는 Claude compatibility mirror
  - SD-12: adapter 간 상하관계 금지; 공유 변경은 portable-first 후 세 realization 동시 검증
  - SD-13: 하나라도 deferred/unverified면 전체 PARTIAL; 세 runtime active surface+fallback+footprint 검증 때만 GREEN
  - SD-14: bootstrap <=16384 bytes/adapter, active skill metadata <=7000 chars/surface, duplicate activation 0
  - SD-15: normal/unknown/repeated hook injection 0 bytes; validated tight/critical transition만 1회 <=240 UTF-8 bytes; quality floor 불변
  - SD-16: baseline 대비 >5% footprint 회귀 실패; production paired n>=30 전 절감률 주장 금지
  - SD-17: primary routing handshake = model proposal → user confirmation → capability-owned execution
  - SD-18: fixed five-field confirmation card; agent-filled, one line per field
  - SD-19: observable applicability predicates; read-only exempt; direct is explicit
  - SD-20: one confirmation per top-level intent; material route/scope/completion/risk change만 재확인
  - SD-21: main-session context is the primary optimization target; owner/worker reads detail
  - SD-22: harness-manifest owns invocation/routing metadata; policy TSV is generated
  - SD-23: primary candidate set = entry-router only
  - SD-24: generic/circular trigger descriptions fail conformance
  - SD-25: core-first then three sibling runtime projections in one cycle
  - SD-26: worker input = one kernel + exactly one type + assigned contract
  - SD-27: worker return = artifact/verdict/blocker exact three-line envelope
  - SD-28: worker output is machine handoff; user explanation belongs to main
  - SD-29: lifecycle suppression, prompt isolation, physical masking are separate claims
  - SD-30: safety/scope/route/verification/depth/model contracts are not pruning targets
  - SD-31: static bootstrap bytes only; no aggregate token/cost savings claim
  - SD-32: orientation/semantic precedence 뒤 명확히 matching entry-router가 primary
  - SD-33: direct는 선택된 entry의 intensity이며 capability-free 우회가 아님
  - SD-34: explicit code/paper/doc analysis+usable analysis 부재이면 read-only/no-files 명시가 없는 한 analyze-project가 기본 primary
  - SD-35: implicit Skill matching은 description 기반 model judgment; deterministic activation 미주장, bootstrap+manifest+projection+fixture로 reliability 보강
open_decisions: []
decisions_resolved:
  - D1: runtime gate 결과 확정 — slash 호출만 생존, parent Skill-tool·실파이프는 차단. 13개는 parent-invoked로 model invocation 유지
  - D3: 영문 "Use when…" 트리거 병기 (사용자 확정 2026-07-13) — 한국어 blurb 유지 + 영문 트리거 추가, entry-router 한정 (대체 아님)
  - D4: 2026-07-16 사용자 확정 — model-invoked entry를 유지하되 fixed execution confirmation을 먼저 받고, full detail은 owner/worker가 읽어 main context를 보호
