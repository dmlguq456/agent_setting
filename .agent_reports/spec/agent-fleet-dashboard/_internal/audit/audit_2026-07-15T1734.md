# Audit Report — agent-fleet-dashboard (spec v8 + minor 6건 ↔ 현행 코드)

- **Date**: 2026-07-15 17:34
- **Type**: documents 등가(spec) — v3 선례(`audit_2026-07-12T0910.md`)를 따른 spec 대상 확장
- **Scope**: all (사용자 지정 중점: forward 계약 실재 / minor 6건 방향 일치 / v7 소급 기록)
- **P1 baseline**: v8 원본 = git `8dd0c062` (2026-07-15), 누적 minor 6건

## Summary

| Aspect | 🔴 Critical | 🟡 Warning | 🟢 OK |
|---|---|---|---|
| 상태 신선도 (state/summary) | 1 | 0 | — |
| forward 계약 실재 (spec→code) | 0 | 1 | 9 |
| 상호참조 (§9/§10, 버전 이력) | 0 | 1 | 2 |
| P1 누적 drift | 0 | 0 | 1 |

**Total**: 🔴 1 / 🟡 2 / 🟢 12

## Perspective 1 — Accumulated Minor Drift vs v8 원본

### 1.1 Accumulated Minor Entries

| # | 내용 | 방향 |
|---|---|---|
| #1 | 제목 provider 4~8단어·64자 소급 동기화 (`80c492e9` 코드 선행의 사후 교정) | spec←code |
| #2 | wide name zone 고정 상한 40열 복원 | spec→code |
| #3 | F-29 native 서브 에이전트 관측 등재 (구현 후속 명시) | spec 신규 |
| #4 | unused의 stale 창 면제 (사용자 결정) | spec→code |
| #5 | kill 키 s/x 확정 + §9 `control.py`·`model.py` 등재 | spec←code(사용자 확인) |
| #6 | parent_cwd 래퍼 정합 (cross-harness orphan 해소) | spec←code |

### 1.2 Diff Summary

- **변경량**: 약 +16 / -4 줄 (취소선 개정 방식 — 원문 보존)
- **핫스팟**: 없음 (전부 국소 개정)
- **판정**: 6건 전부 minor-log와 1:1 대응, 미기록 drift 0. 🟢

### 1.3 Correlation with Perspective 2

| P2 finding | 대응 minor | 판정 |
|---|---|---|
| 상태 신선도 🔴 | 없음 — v8 major 시점 기록이 구현 완료 후 미동기 | harvest 마무리 누락 |
| 어휘 매핑 표 부재 🟡 | 없음 — v8 major 원문부터 존재 | pre-existing (v8 태생) |
| §10 다이어그램 🟡 | minor #5가 §9만 갱신 | 부분 이행 |

## Perspective 2 — Universal Principles + 코드 정합

### Aspect: 상태 신선도

#### 🔴 pipeline_state.yaml `dev: pending` — 구현 완료 후 미동기
- **File**: `spec/agent-fleet-dashboard/pipeline_state.yaml:14`, `pipeline_summary.md:5`
- **Detail**: F-25~F-27 구현이 `plans/2026-07-15_fleet-v8-reliability`에서 완주·머지(테스트 416)됐는데 state는 "v8 구현 미착수", summary Status는 "구현은 별도 사이클"로 남음. §0.1 오리엔테이션이 이 파일을 1순위로 읽으므로 새 세션이 잘못된 상태를 상속한다. summary의 v8 implementation closure 섹션도 부재(v5/v6 선례 대비 누락).
- **Introduced**: v8 major 작성 시점 기록이 harvest에서 갱신 누락 (main 오케스트레이터 몫)
- **Suggested fix**: dev=done(F-25~F-27, F-28/F-29 후속 대기)로 동기화 + closure 섹션 추가 — 즉시

### Aspect: forward 계약 실재

#### 🟡 F-25 "상태 어휘 매핑 표 규범 명문화" 미이행
- **File**: `spec/agent-fleet-dashboard/prd.md` §4.8 F-25 어휘 정합 항
- **Detail**: spec은 "매핑 표를 spec 규범으로 명문화"를 계약으로 명시했으나 표 자체는 spec에 없고 코드(`model.py` LIVENESS_STATES·`_session_status_state`)와 plan 문서에만 존재. v8 사이클 follow-up #3("registry busy + 48h 침묵 → stale" 규범 행 추가) 역시 같은 표에 반영될 항목.
- **Introduced**: v8 원문부터 (pre-existing)
- **Suggested fix**: v9 major에서 매핑 표 삽입 + follow-up #3 행 포함

#### 🟢 실재 확인 9건
`classify_session/classify_job/state_evidence/hysteresis`(F-25) · `read_registry`+`◌`/unused/provenance(F-26) · `control.py` 전 함수+action log+s/x 커서(F-27) · Codex fd 소유권(F-24, `codex.py` 6개소) · 제목 4~8단어·64자(#1) · `_NAME_WIDE_MAX=40`(#2) · stale 면제+테스트 2건(#4) · §9 등재(#5) · 래퍼 3종 parent_cwd+SD-15 PASS(#6). F-29 코드 부재는 "구현 후속" 명시와 일치 — drift 아님.

### Aspect: 상호참조

#### 🟡 §10 컴포넌트 다이어그램에 `control.py` 노드 부재
- **File**: `spec/agent-fleet-dashboard/prd.md` §10
- **Detail**: §9 모듈 트리에는 등재됐으나 다이어그램 미반영. F-27은 fleet 최초의 signal/write 표면이라 "read-only 관찰 소스" 프레임에 예외 노드로 명시할 가치가 있음(§0.5 v8 경계 개정과 짝).
- **Suggested fix**: v9 major에서 다이어그램 갱신

#### 🟢 v7 소급 기록 정합
Version History의 v7(F-24) 소급 항목이 스냅샷 실체(`_internal/versions/v6/prd.md`)·코드(F-24 구현)와 일치.

## Verdict

- **Status**: 🔴 requires attention (1건 — 상태 신선도, 즉시 조치 가능)
- **Recommended next action**: ① state/summary 동기화(즉시) ② 나머지 🟡 2건은 v9 major에 흡수해 처리
- **Baseline reset recommendation**: minor 6건 ≥ 5 — **v9 major 권고**: minor 6건 흡수 + 어휘 매핑 표 삽입(follow-up #3 포함) + §10 다이어그램 갱신 + kill 조작 모델 마우스 기반 재설계(사용자 방향 2026-07-15 "그냥 마우스로 처리") + F-29 구현 착수를 한 패키지로.

---

> Generated by `/audit`. The audit report itself applies no artifact edits.
