project_name: harness-installer
created: 2026-07-12
mode: [cli]
scope_note: "하네스 배포 표면 — managed release(일반 사용자) + linked checkout(maintainer) + 선택적 plugin compatibility. clone 없는 bootstrap, automatic packaged update, INSTALL_LAYOUT 배선을 `harness` CLI가 소유한다."
phases:
  spec: done                # v1 PRD 확정 (2026-07-12 — 하이브리드 구조·`harness` 명령 사용자 확정, currentness 검증 반영)
  scaffolding: done         # 브랜치 9792fd3 → main 머지 완료(4de50f6, 사용자 신호 2026-07-13)
  dev: done                 # 사이클 1(51/51)+2(53/53)+3(29/29, hook 5종 완결)+quick fix 2건(verify 채널-인지·liveness pid) — 전부 main 머지
  release_delivery: done         # v5 release-bound bootstrap published and verified as v1.0.1
last_updated: 2026-07-14
spec_version: 6
inputs:
  - .agent_reports/research/cross-platform-agent-frameworks/
  - .agent_reports/analysis_project/harness-alignment/
  - .agent_reports/spec/harness-layer-sync/ (interlock)
  - INSTALL_LAYOUT.md (실측·대체 대상)
  - runtime-currentness 검증 2026-07-12 (Claude/Codex/OpenCode plugin surface 공식 문서)
  - root README 공개 진입점 진단 + GitHub reference brief 2026-07-13
decisions_locked:
  - INST-D-1: 2-채널 하이브리드 — plugin 채널(소비) + installer CLI(dev·runtime-owned 표면), 병존 (사용자 확정 2026-07-12)
  - INST-D-2: CLI 진입 명령 = `harness` (한 단어 + 서브명령, fleet 동형; 사용자 확정 2026-07-12)
  - INST-D-3: hash-manifest 는 installer 가 복사한 파일만 추적(소유 경계) — HLS 와 스키마 공유, GSD bin/install.js 정독 선행 게이트(HLS §3.2 공유)
  - INST-D-4: plugin 내용물 SoT = capabilities/·roles/ + sync-native 생성기 — 손복사 금지, Claude plugin cache 의 self-contained 요구는 빌드 시점 생성물 포함으로 충족
  - INST-D-5: Codex plugin 은 agents(.toml) 미탑재(공식 한계) — symlink projection 병행 유지
  - INST-D-6: plugin 탑재 hook — 채택 5종 완결(사이클 3: spec 파이프 3종 AGENT_HOME=PLUGIN_DATA env-prefix 재기준, canonical 무수정) / 제외 memory·statusline·dispatch·core-first 계열
  - INST-D-7: verify 는 채널-인지 — plugin 채널 미채택 머신에서 plugin check 명시 SKIP(ok), 등록+미설치·drift 는 실패 유지 (parity-loss 원칙)
  - INST-D-8: root README = human-owned product landing page — 가치/설치/사용/런타임 차이를 앞세우고 내부 전수 지도는 canonical deep docs로 위임 (사용자 확정 2026-07-13)
  - INST-D-9: sync-skills portable capability와 현역 runtime projection 퇴역 — prose hash sync 대신 manifest/projection/adaptation/conformance 결정론 검사 유지 (사용자 확정 2026-07-13)
  - INST-D-10: 일반 사용자 기본은 checksum-verified managed packaged release; linked checkout은 maintainer 경로
  - INST-D-11: supported OS automatic update + staging/checksum/rollback + atomic pointer
  - INST-D-12: managed updater는 linked/foreign source를 fetch/pull/repoint하지 않음
  - INST-D-13: README 상단은 plugin 경계 대신 간결한 제품 강점을 제시
  - INST-D-14: bootstrap code와 archive는 동일 immutable release tag에 결속; raw main은 public install trust path가 아님
  - INST-D-15: release-relevant main 변경은 deterministic SemVer policy로 자동 release; docs/report/CI/test-only는 skip, unclassified behavior는 patch
open:
  - INST-OPEN-4: OpenCode 복수형 migration — 로컬 1.17.13 단수형 정상, drift-watch check 상시 감시, 버전업 시 별도 사이클
