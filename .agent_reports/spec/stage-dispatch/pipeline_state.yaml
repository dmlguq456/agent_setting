project_name: stage-dispatch
component: stage-dispatch          # autopilot 파이프 디스패치 토폴로지 개정 (repo spec/prd.md = Unified Memory System, spec/harness-layer-sync·dispatch-profiles·agent-fleet-dashboard 무관)
created: 2026-07-10
mode: [library, cli]              # library(스테이지 분사 계약·오케스트레이션) + cli(dispatch wrapper·jobs.log·fleet 관제) — infra
scope_note: "각 sub-skill 스테이지(code-plan/execute/test/report)를 standard+ 에서 기본으로 별개 headless 세션 분사. depth-1 owner = 얇은 conductor, 인터페이스 = 산출물 파일만(산출물 기반 소통). 2026-07-06 'owner 단일 세션 + in-session 팀' 기본값의 명시적 반전."
inputs:
  - 사용자 결정 2026-07-10: "스킬 단위 처리 분사를 기본 지침으로. 어차피 산출물 기반 소통." (입도=sub-skill, 적용=standard+)
  - research/cross-platform-agent-frameworks/analysis_summary.md   # §4-(8) fresh-context-per-agent + file-state 지배 관용구
  - research/cross-platform-agent-frameworks/cards/gsd.md          # fresh-context subagent per stage·.planning file-state·two-stage routing·size-budget
  - research/cross-platform-agent-frameworks/06_implementation.md  # 파일-복제 회피·parity 정직성
  - 현행 계약 실측 2026-07-10: core/OPERATIONS.md §5.10·§5.8 / CONVENTIONS.md §1·§2 / WORKFLOW.md §1.1·§5 / DESIGN_PRINCIPLES.md §8 / skills/autopilot-code references(dev-pipeline·context-and-guards) + sub-skills / adapters/claude/bin/dispatch-headless.py / adapters 3종 bootstrap §0(C)
  - 운영 실증 2026-07-10: ① in-session 서브에이전트 jobs.log 미등록(fleet 관제 불가시) ② hook ceremony 미수령 ③ owner 단일 세션 컨텍스트 비대
phases:
  spec: done              # PRD v1 — SD-1~9 채택 + SD-OPEN-1(inline 임계 pilot 계측). 구현 2-phase 분할 §12
  scaffolding: deferred   # lean — 코드 skeleton 은 autopilot-code 로 이월 (§11 module 구조만 확정, wrapper 재작성 불요)
  design: n/a             # 시각 UI 없음 (인프라 계약)
  dev: pending            # autopilot-code 가 누적 (Phase 1: 계약문서+wrapper증분+pilot / Phase 2: autopilot-* 확산+drill 회귀)
last_updated: 2026-07-10  # v1 초안
decisions_locked:
  - SD-1: depth-1 owner = 얇은 conductor, 스테이지(plan/execute/test/report) = depth-2 headless 세션 (conductor 는 verdict/게이트만, 스테이지 본문 미독)
  - SD-2: 인터페이스 = 산출물 파일만, 세션 간 대화 컨텍스트 전달 금지, 계약 완결성 의무 (산출물 기반 소통 명문화)
  - SD-3: 스테이지 세션 jobs.log depth=2,parent=conductor,worker_role=sub-skill,owner=autopilot-code 등록 → fleet 스테이지 row. stealth-death 가드 conductor 책임
  - SD-4: depth-2 write 계약 = 스테이지-워커 클래스별 소유(plan/execute소스/test/report). depth 3+ 금지 유지(스테이지 세션 내부는 in-session 팀만)
  - SD-5: 스테이지 model role = conductor 가 CONVENTIONS §2 매핑으로 명시 (wrapper 암묵 선택 금지, §5.10 ⑦)
  - SD-6: 동시상한5 = Σ(conductor+활성스테이지). 마이크로-스테이지 inline. 실패=스테이지만 재분사(산출물 재사용). lock 은 report 의 pipeline_summary 쓰기만
  - SD-7: 영향 표면 14곳(§9) — core·bootstrap·SKILL·wrapper·fleet·drill. 문구 편집은 소유 스킬(core-first) 별도
  - SD-8: 적용 = standard+ 기본, direct/quick = 현행 inline 유지. 2026-07-06 'owner 단일 세션' 기본값 명시 반전
  - SD-9: wrapper 재작성 불요 — depth=2/parent/worker_role 이미 지원. stage-dispatch helper 신설은 pilot 후 판정
decisions_open:
  - SD-OPEN-1: 마이크로-스테이지 inline 손익 임계(어느 스테이지 크기부터 분사 이득). research per-stage cost 미수치화 → Phase 1 pilot 토큰/시간 계측으로 확정
