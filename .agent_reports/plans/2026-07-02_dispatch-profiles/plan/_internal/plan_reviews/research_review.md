# Plan Review — 연구팀 (user-proxy axis)

- **Date**: 2026-07-02
- **Plan**: `.agent_reports/plans/2026-07-02_dispatch-profiles/plan/plan.md`
- **Spec**: `.agent_reports/spec/dispatch-profiles/prd.md`
- **Task type**: infra/config (dispatch 인프라 — family-level naming · cross-file scope · convention fit · downstream 가중). **user-proxy 축 한정** — code correctness 는 품질관리팀 r1/r2 (6 issue) 에서 해소 완료, 재검토 안 함.
- **Memo count**: 6 (🔴 1 · 🟡 3 · 🟢 2)

> Knowledge source 참고: `analysis_project/`·`research/` 디렉터리 부재 — 본 리뷰는 spec(prd.md)·live repo(roles/README·core/ADAPTATION.md·INSTALL_LAYOUT.md·check-adaptation-boundary.sh·tools/fleet/·core/MEMORY.md)·agent memory 에만 근거. paper-grounding·domain-science 축은 본 task 에 무해(해당 없음).

## Memos

| # | Location | Axis | Memo | Rationale (근거) |
|---|---|---|---|---|
| 1 | Phase 5 (fleet) | 🔴 [sync-downstream] | spec §7 fleet row 는 **두 가지**를 요구한다 — (a) `.dispatch/homes/*/` **추가 스캔 루트**, (b) `·profile` 태그. plan Phase 5 는 (b) + profile field/backfill 만 커버하고 **(a) 를 누락**. fleet `_job_liveness()` (`tools/fleet/collectors/dispatch.py:120`) 는 모든 job 을 `_proj_home()/projects/<enc>` 아래에서만 찾으므로, transcript 가 `homes/<slug>.<name>/projects/` 로 격리된 profile job 은 fleet view 에서 **항상 DEAD 로 오판**된다 — Phase 4 가 `dispatch-liveness.sh` 에 대해 고치는 것과 **동일한 실패**를 fleet 쪽엔 안 고침. | spec §7 "fleet collectors … `.dispatch/homes/*/` 를 추가 스캔 루트로". Phase 5 는 "fleet profile exposure" 를 표방하나 liveness column 이 profile job 전부에서 틀려 monitoring 목적이 무력화. **planned change 안의 버그가 아니라 spec §7 이 명시한 산출물이 빠진 scope-completeness gap** → user-proxy 축. **fix**: Phase 5 에 `_job_liveness`/`_proj_home` 를 profile-aware 로 (Phase 4 와 동형: `profile=` 있으면 `homes/<slug>.<name>/projects/<enc>`) 확장하는 step 추가 + mirror. |
| 2 | Phase 0 / Current State | 🟡 [sync-downstream] | 신설 top-level `profiles/` 가 어떤 **projection surface 목록에도 등록 안 됨** — `INSTALL_LAYOUT.md:25` claude projection loop (`for p in … skills agents … hooks utilities tools scaffolds loops …`), codex/opencode allowlist loop (`check-adaptation-boundary.sh:99/125/270/409`), `check_projection_entry_allowlist`. tool 자신(`adapters/claude/tools/profile/build-home.py`)은 `tools` symlink 로 runtime home 에 투영되지만 **그 입력 카탈로그 `profiles/` 는 투영 안 됨** → runtime-home(`~/.claude`) 컨텍스트에서 build-home 실행 시 `<AGENT_HOME>/profiles/` 부재로 실패. | 정상 흐름(dispatch = repo/worktree 컨텍스트, AGENT_HOME=repo root)에선 동작하나 plan 이 skills/agents/settings 는 runtime-first/repo-fallback 을 세심히 다루면서 `profiles/` 는 침묵. **fix**: (i) `profiles/` 를 repo-context-only build input 으로 명시(의도적 non-projection — allowlist 가 거부하는 것과 정합) 하거나 (ii) projection surface + allowlist 에 추가. 미기재 시 향후 projection-guard drift·runtime-home 호출 surprise. |
| 3 | Phase 0.1 (README) | 🟡 [sync-downstream] | spec §9 는 `profiles/README.md` 를 "**sync-skills 자동 갱신 후보**" 로 명시하나 plan 은 **sync-skills 를 한 번도 언급 안 함**. sync-skills 는 Skills+Agents 정의에서 GitHub `README.md` 대시보드(워크플로우 map + cheat-sheet)를 동기화 (`capabilities/sync-skills.md:17`); `profiles/` 는 skills/roles/capabilities 와 동급의 신설 카탈로그 surface. | plan 의 downstream 분석은 manifest/projection guard 엔 철저하나 sync-skills/README 대시보드 연동을 빠뜨림. **fix**: scope-in(대시보드에 profiles 카탈로그 노출) 하거나 명시적으로 defer(§8 처럼 별도 사이클) — 침묵은 drift 표면. |
| 4 | §3 / Phase 0.4 / 전역 | 🟡 [naming-conflict] | **"profile" 은 이미 확립된 family 용어** — user-personalization 레코드(`mem profile <stem>`, DB `type=profile`, "사용자 프로필", analyze-user/post-it; `core/MEMORY.md §7.6`, `core/CORE.md:133`). 신설 "dispatch profile"(`profiles/`, `--profile`, `profile=` k=v)이 **같은 단어를 전혀 다른 개념**(masked config home)에 재사용. | 컨텍스트(`mem profile` vs `--profile`/`profiles/`)가 대개 구분해 주나, 두 "profile" 이 공존하면 문서·회상·grep 에서 혼선. profiles/README 가 roles/ 와의 구분을 명시하듯 **mem-profile(사용자 프로필)과의 구분도 한 줄 명시** 권장. blocking 아님. |
| 5 | Phase 5.2 | 🟢 [scope-creep] | "mode+profile backfill for proc jobs" 중 **mode backfill 은 spec §7 이 요구하지 않은 기회주의적 추가** (proc job 의 `mode=None` 보정). phase 헤더에 명시돼 은닉은 아니고 low-risk 이나 spec 범위 밖. | spec §7 fleet 요구는 profile 노출 + 스캔 루트뿐. mode backfill 은 별개 개선 — v1 blast radius 최소화 원칙(§8 m2) 관점에선 별 사이클이 더 깔끔. 유지해도 무방하나 "spec-mandated 아님"을 plan 이 표기하면 리뷰어/사용자가 인지 가능. |
| 6 | Phase 0.1 / 0.2 | 🟢 [convention/template-realism] | (a) profiles/README 의 DP-3 예외 서술은 roles/ 와만 대비 — canonical 경계 규칙 `core/ADAPTATION.md:75` ("Concrete model names belong in adapter documents") **를 이름으로 앵커**하면 roles/README:39 파생이 아닌 규칙 자체에 결속돼 더 견고. (b) Step 0.2 guard 나열("artifact-guard, git-state-guard, builtin-memory-guard")이 spec §5 의 `spec-gate` 를 빠뜨림 — hooks/ 전체가 hard-link 되므로 실효엔 무해(포인터 성격)하나 spec §5 guard **set** 과 일치가 낫다. (c) build-home 가 `harness ∈ {claude,codex,opencode}` 를 통과시키나 `bootstrap-opencode.md` 없음 → `harness:opencode` 선언은 schema 통과 후 template-read 실패(v1 에 opencode profile 없어 무해, latent). | 전부 minor. Q2 boundary-rule wording·Q4 template contract 정합의 마감 다듬기. |

## Overall Assessment

spec §9 module-block(profiles/README·yaml·fragments·templates{claude,codex}·tools/profile/build-home.py·양 dispatch wrapper·gitignored `.dispatch/homes/`)과 §7 monitoring 확장 대부분을 정확히 커버하고, opencode(P1)·§8 hybrid-routing 문서를 올바르게 scope-out 했으며 mirror/projection-guard/manifest downstream 도 성실히 짚었다 — expose 타깃(skills autopilot-lab/analyze-project/post-it, agents dev-team/qa-team/material-team)·`model_role: fast implementer`(`core/ADAPTATION.md:68` 유효 portable role)·bootstrap 템플릿 L0 계약(core-4 Read·guards·depth-1·no-L1)은 전부 실재·정합. **단 하나의 blocking gap**: spec §7 fleet row 가 명시한 `.dispatch/homes/*/` 스캔 루트(=fleet liveness 의 profile-aware 화)를 Phase 5 가 누락해 profile job 이 fleet 에서 DEAD 로 오판된다(Memo 1, 🔴) — Phase 4 와 동형 확장 step 추가로 해소. 나머지는 projection surface 등록/sync-skills 연동 명시(🟡)와 "profile" 용어 중복 주석(🟡), 다듬기 2건(🟢).
