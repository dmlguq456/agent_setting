# Pipeline summary

완료. Claude `deep orchestrator`는 `opus/xhigh`로 고정됐고 Fable은 대화형
depth-0 메인 세션에만 남았다. registered headless, inherited dispatch, native
subagent/fork 및 capacity 후보는 Fable을 fail-closed한다.

supervisor는 모든 종료 경로에서 exact attempt를 terminal로 reconcile하며 Claude
429는 `dead-capacity`로 보존한다. post-exit watcher는 orphan cascade, typed
supervisor envelope, exact-PID fallback 순서로 처리한다. parent lookup은 linked
worktree의 canonical repository identity를 인식하고 Fleet/park/wait는 공통 observed
liveness 판정을 소비한다. Fleet는 stale open owner를 mutation 없이
`reconcile-needed`로 표시한다.

통합 검증은 portable guards `358/358`, canonical Fleet `872/872`, native model
hook `15/15`, supervisor·contract·mode·fallback 집중 회귀, generated projection,
adaptation boundary, strict runtime projection 및 `doctor --runtime`을 통과했다.
0-token drill g9은 exact Codex+Claude strong depth-2 two-way batch의 overlap/Fleet/
provenance/reap을, g10은 OpenCode depth-2의 exit 69 및 zero side effect를 확인했다.

`main`과 `origin/main`은 `6fd83309`까지 반영됐다. checked cleanup은 active PID,
stale registry row, artifact harvest obligation이 모두 0인 상태에서 worktree를
`status=removed`로 정리했다.
