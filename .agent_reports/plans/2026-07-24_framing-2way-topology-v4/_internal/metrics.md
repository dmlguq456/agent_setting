# metrics — framing-2way-topology-v4

- 실행 형태: inline (dispatch-infrastructure self-modification —
  registry/validator/route compiler 자체 개정; `STAGE_DISPATCH_INLINE_OK` 사유).
  topologies.json·capability_topology.py·capability-route.py를 고치는 작업을
  그 기계 위에서 분사하면 개정 대상 계약과 실행 계약이 뒤엉킨다.
- 근거 지침: 사용자 2026-07-24 "모든 topology 전반에 대한 업데이트로서 내가
  근본적으로 뭘 원하는지만 명확히 파악하면 된다" — framing 독립 2-way의
  registry 전반 적용 승인.
- 이름 결정: pre(부팅 토큰과 충돌)·diagnose(결함 함의)·analyze(analyze-* 충돌)·
  identify(판정 부재) 검토 후 frame 확정. 방향 판정 강제는 이름이 아니라
  unit 계약(판정문 필수)으로.
