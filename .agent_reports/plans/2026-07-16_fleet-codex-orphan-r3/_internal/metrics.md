# Execution metrics

- route: `autopilot-code / debug / standard`
- pipeline: `code-plan -> code-execute -> code-test -> code-report`
- inline exception: registered depth-1/broker/native fallback is being repaired by another active session, and this task modifies the Codex dispatch wrapper itself. Repeating that owner path would overlap the active remediation and risk the observed empty-wait stall. The approved cycle therefore executes the four stages sequentially in this isolated worktree.
- failure class kept out of scope: `native-fallback-child-not-created / empty-wait-loop`
- assurance compensation: focused wrapper test, ambiguous-cwd render test, full Fleet suite, mirror parity, portable guard, integrated rerun.
- result: focused/integrated checks pass; portable-guard failure delta versus unmodified main is zero (same six baseline expectation drifts).
