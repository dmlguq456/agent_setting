# Verification — registered-dispatch completion join

## Verdict

PASS. The runtime-owned completion join, Codex App Server supervisor, Claude
same-session supervisor, two-phase parent guard, exact orphan cleanup, and
terminal normalization satisfy the SD-78 acceptance contract.

## Focused regression evidence

All commands ran through the Codex `verification-runner` contract with
`PYTHONDONTWRITEBYTECODE=1`.

| Scope | Result |
|---|---:|
| exact batch join | 7/7 |
| Codex App Server supervisor | 3/3 |
| Claude session supervisor | 3/3 |
| Claude command-scoped parent guard | 3/3 |
| Codex two-phase parent guard | 6/6 |
| terminal inspection | 14/14 |
| orphan watcher | 2/2 |
| Codex dispatch wrapper | 22/22 |
| Claude dispatch wrapper | 17/17 |
| cross-adapter dispatch v11 | 10/10 |
| parent-context conformance | 3/3; 34 captures checked |

The supervisor fixtures prove that two exact sibling attempts are joined as
one batch outside the model, no raw child payload enters the continuation,
the same Codex thread / Claude session resumes once, supervisor state is
removed, and only one final terminal handoff is emitted. The phase-guard tests
prove that opening sibling 1 still permits exact same-parent registration of
sibling 2 while reads, waits, source/git inspection, foreign dispatch, and
unrelated tools remain denied.

## Repository-wide evidence

- `hooks/portable-guards.test.sh`: `PASS=356 FAIL=0`, `status=ok`.
- `tools/check-adaptation-boundary.sh`: passed; only the existing documented
  concrete-reference warning remained.
- `git diff --check`: passed.
- Existing dispatch contract, lifecycle, liveness, harvest, completion-marker,
  concurrency, wait, route, and stage-capacity suites passed earlier in the
  same implementation cycle.

An earlier portable-guard run reported 354/356 because a diagnostic invocation
had created an untracked `adapters/codex/bin/__pycache__`. The boundary checker
correctly rejected that cache. Removing that generated bytecode and rerunning
the unchanged source produced 356/356, confirming an environmental
contamination rather than a product-code failure.

## Runtime and parity evidence

- Installed Codex App Server completed a live two-turn same-thread smoke.
- Installed Claude Code completed a live same-session resume smoke.
- Claude Code 2.1.218 command-scoped `--settings` with wildcard `PreToolUse`
  denied a Bash call as `permission-rule`; no user settings were changed.
- Independent registered-headless Claude audit round 3 reported PASS with no
  high/medium findings across the four enforcement points. The report is at
  `_internal/dev_reviews/round_3.md`.

The audit worker's final prose did not satisfy the exact three-line terminal
envelope, so its Fleet terminal receipt was rejected and the row was explicitly
closed. The report content remains valid review evidence; no false terminal
PASS is claimed.

## Final runtime state

- `headless_open_jobs=0`
- `orphaned_conductor_jobs=0`
- session liveness: `open 0 ; alive 0 ; suspect/dead 0`
- attempt-scoped supervisor-state files: 0
