# Skill-review raw findings


## claude-skill-setting

**key_files**: /home/Uihyeop/agent_setting/adapters/claude/skills/autopilot-code/SKILL.md; /home/Uihyeop/agent_setting/adapters/claude/skills/audit/SKILL.md; /home/Uihyeop/agent_setting/adapters/claude/skills/code-plan/SKILL.md; /home/Uihyeop/agent_setting/adapters/claude/skills/post-it/SKILL.md; /home/Uihyeop/agent_setting/adapters/claude/skills/autopilot-code/references/owner-execution.md; /home/Uihyeop/agent_setting/adapters/claude/skills/autopilot-code/references/dev-pipeline.md; /home/Uihyeop/agent_setting/harness-manifest.json; /home/Uihyeop/agent_setting/capabilities/topologies.json; /home/Uihyeop/agent_setting/core/WORKFLOW.md; /home/Uihyeop/agent_setting/adapters/claude/agents/plan-team.md; /home/Uihyeop/agent_setting/adapters/claude/agents/qa-team.md; /home/Uihyeop/agent_setting/adapters/claude/CLAUDE.md

**current_structure**: 27 capability skills live 1:1 under adapters/claude/skills/<name>/SKILL.md, generated from harness-manifest.json (SKILL.md:1-2 "GENERATED METADATA — edit harness-manifest.json"). Each SKILL.md frontmatter: name (the Skill-tool invocation key), description, argument-hint, metadata {group, fam/family, invocation_class, modes, blurb, use_when, not_for}. By (group,invocation_class): entry/entry-router = 10 autopilot-* families; ops/entry-router = audit; sub/parent-invoked = code-plan/execute/test/report/refine + design-* + draft-* (13 stage units); pre/entry-router = analyze-project/analyze-user; ops/model-support = post-it.

An 'entry skill' concretely = group:entry, invocation_class:entry-router (manifest autopilot-code confirms). Its SKILL.md body is explicitly 'a compact pre-approval entry router... intentionally contains no execution procedure' (autopilot-code/SKILL.md:18-20; identical in audit/SKILL.md:18-20). Body = Pre-approval boundary + Post-approval owner contract + a single-row Reference Index pointing to ONE owner-execution.md edge (autopilot-code/SKILL.md:36-40). autopilot-code exposes modes [dev,debug,audit] selected by --mode.

Sub/stage skills are heavier and contain real procedure: code-plan/SKILL.md dispatches native subagent plan-team (lines 36-49) and qa-team (line 68). Native subagents live at adapters/claude/agents/*.md (9 files: plan-team/기획팀, qa-team/품질관리팀, dev-team, editorial-team, design-team, material-team, research-team, memory-scout, codex-review-team); each a named-persona Task subagent (plan-team.md:1-10 'Called from code-plan and code-refine skills — not directly by the user'). Manifest wires them via requires.capabilities (stage units) + requires.roles (native teams).

**routing_today**: Routing decision is made at the MAIN AGENT via core docs, NOT inside a skill. Flow: (1) user request → main reads core/WORKFLOW.md §0.2 Semantic Primary Routing — fixed precedence list + request-shape tables (WORKFLOW.md:91-124) — to pick ONE primary entry capability from compact manifest metadata (§0.4:190-196 'choose from compact manifest routing metadata and §0.2; do not load the full entry Skill body'). (2) Main presents the §0.4 five-field confirmation card and waits for approval (WORKFLOW.md:147-202). (3) On approval the Skill tool invokes the entry skill by frontmatter name; the entry skill body directs the owner to load capabilities/<name>.md then references/owner-execution.md (autopilot-code/SKILL.md:29-40). (4) --mode + --intensity select the stage graph: owner-execution.md:33-43 maps intensity→topology; direct/quick stay inline, standard+ dispatches each durable stage unit (code-plan/execute/test/report) as its own dispatch-depth-2 headless session via utilities/dispatch-node.py (dev-pipeline.md:12-31,81-101). The depth-1 conductor is 'a thin conductor' passing only artifact paths and reading only verdict/gate state (dev-pipeline.md:9,31). Two deterministic load phases (WORKFLOW.md:198-202): manifest metadata pre-approval, portable owner contract post-approval. Routing happens at entry (main + entry skill), never at depth-2 — depth-2 stage workers only read files and run their one stage contract.

**fits_direction**: STRONG alignment on several axes. (1) entry-router is a first-class class (invocation_class:entry-router in every autopilot-* frontmatter + manifest); entry SKILL bodies are deliberately thin routers with 'no execution procedure' (autopilot-code/SKILL.md:18-20) — matches 'routing at ENTRY.' (2) Routing never happens deep: the depth-1 conductor passes only paths and reads only verdicts, depth-2 units just execute one contract (dev-pipeline.md:9,31,101) — matches 'never route at dispatch depth-2.' (3) capabilities/topologies.json is an explicitly COMPOSITIONAL recipe layer: 11 recipes keyed by capability, each with topology_class + a node DAG (nodes with id/kind/depends_on/inputs/outputs/write_scope/dispatch_depth/fallback_hops). Stages compose into a topology rather than a per-request static blob — matches 'a recipe that COMPOSES units.' (4) Units = the sub/parent-invoked skills (code-plan/execute/test/report), each a composable dispatchable atom executed via dispatch-node.py at dispatch_depth 2 — matches 'a UNIT = composable dispatchable atom; all tracked work is a dispatched unit.' (5) A family-label field already exists and is non-runtime: metadata.fam / manifest.family (fam:code on code-plan, family:code on autopilot-code) is a pure grouping label — the natural home for 'TEAM = family label only.' (6) A judgment step partially exists: §0.2 semantic precedence + mode inference ('infer debug when the request is centered on an error log', owner-execution.md:29) + human-gated ambiguity ('ask when it is genuinely ambiguous', WORKFLOW.md:364).

**breaks_or_gaps**: (1) NO single entry-skill router. 'THE entry skill is the router' is realized as 10 sibling entry-router skills PLUS main-agent prose in core/WORKFLOW.md §0.2 (WORKFLOW.md:91-124). Case selection lives as a fixed precedence list + request-shape tables read by the main agent, not as a composing entry skill — the router is core docs, and each entry skill only routes WITHIN its own family. (2) 'team' TERMINOLOGY COLLISION / BREAK: the direction says TEAM = family label only (not runtime agent) and native subagents = only ephemeral depth-free helpers. But today the *-team names (plan-team/기획팀, qa-team, dev-team) ARE runtime named-persona native subagents doing load-bearing work — plan-team actually authors the plan and code-plan delegates the whole planning procedure to it (code-plan/SKILL.md:36-49; plan-team.md:1-13). So current 'team' = runtime native subagent, directly contradicting both 'team = label' and 'native = ephemeral only.' The label sense the direction wants is instead carried by the separate fam/family field. (3) COMBINATORIAL SEAM at mode granularity: recipes are enumerated per capability×mode (11 recipes; autopilot-spec fixes 6 modes, autopilot-code 3, autopilot-draft/research 3, autopilot-lab appears as 2 rows setup/eval). Modes are fixed enumerated lists in frontmatter+manifest+recipe, so the 'case' axis is a small hard-coded enumeration selected by --mode, not free composition of units into an arbitrary request-specific case. (4) NO long-tail fallback for un-enumerable requests: §0.1 handles read-only orientation and §0.2 is a closed precedence+table; the only 'fallback' in WORKFLOW/CONVENTIONS is dispatch/execution fallback (native/inline when headless unavailable, CONVENTIONS.md:140,514), never a routing-classification fallback for a request matching no enumerated capability/mode. (5) Ambiguity arbitration is human-gated ('ask when genuinely ambiguous', WORKFLOW.md:364), not an automated judgment unit.

**combinatorial_evidence**: capabilities/topologies.json is the decisive artifact and it is MOSTLY COMPOSITIONAL, not a per-case blob. Structure: top-level enumerated vocabularies (intensities[6], execution_topologies[6], worker_kinds[5], transports, tracking_values, inline_reasons[5], fallback_hops) + a `recipes` array of 11 entries. Each recipe = {capability, modes[], topology_class, direct_predicates[], promotion_signals[], quick:{topology,worker_kind,write_scope,dispatch_depth}, standard_plus:{topology, nodes[]}}. nodes[] is a real DAG (autopilot-apply: node apply → node verify with depends_on:[apply], each with kind/inputs/outputs/write_scope/completion_gate/dispatch_depth/fallback_hops). So a case = capability+mode expands into a composed stage graph of dispatchable units — compositional recipe-composes-units, matching the target. BUT the enumeration axis is capability×mode: modes are a CLOSED list per capability (autopilot-code [audit,debug,dev]; autopilot-lab two recipe rows setup/eval; autopilot-spec 6 modes). The claude SKILL frontmatter mirrors this (metadata.modes). So the design composes at the STAGE level but ENUMERATES at the case(capability×mode) level. topology_class is a fixed set {staged, staged+resource, map-reduce, transactional-owner, one-shot-owner, inline}. schema_version:2, rollout.route_compiler:'report-only' with legacy_low_level_dispatch:true indicates an in-progress migration toward a compiled-route model — a route_compiler exists but is not yet authoritative.

**cross_harness_diff**: The skill layer is a pure harness-native PROJECTION of harness-neutral portable sources: SKILL.md files are generated from harness-manifest.json (SKILL.md:1-2), which draws semantics from core/ + capabilities/topologies.json. adapters/claude/CLAUDE.md: 'Claude-native Skills... live under adapters/claude/; skills/ is a compatibility mirror' and 'Portable meaning comes from core/, capabilities/, roles/; map that meaning to Claude-native runtime surfaces.' Claude-specific realizations vs the neutral recipe: (a) a UNIT is realized as a Claude Skill invoked by the Skill tool via frontmatter name; (b) a native TEAM/role is realized as a Claude agents/*.md Task subagent (Korean persona names 기획팀/품질관리팀); (c) dispatch is realized via utilities/dispatch-node.py + stage-dispatch-fallback.py wrappers. topologies.json is capability-level and harness-neutral (transports:[headless,interactive], fallback_hops include same-harness-headless/cross-harness-headless/native-subagent/inline), so cross-harness routing is defined in the portable layer and Claude is one execution_surface among siblings (Codex/OpenCode). Per CLAUDE.md, Claude native subagents are kept DISTINCT from registered recursive headless dispatch; a restriction on one surface never silently extends to the other. Codex/OpenCode adapter files are sibling references, never Claude bootstrap input.

**risks**: Renaming the *-team native subagents to align with 'team = label' collides with heavy existing references: code-plan/refine invoke plan-team, code-execute/report invoke dev-team/material-team, every qa path invokes qa-team — the native-subagent 'team' vocabulary is load-bearing across sub-skills and manifest requires.roles.; The entry-router today is split between thin entry SKILLs and main-agent prose (WORKFLOW §0.2). Consolidating into 'one entry skill that composes' would move classification out of core/ docs into a skill, changing where the §0.4 confirmation gate and the manifest-metadata-only pre-approval load boundary live (WORKFLOW.md:198-202).; Mode is an enumerated closed list in three coupled places (SKILL frontmatter metadata.modes, manifest.modes, topologies.json recipe.modes) all generated from harness-manifest.json; replacing enumerated modes with free composition touches the generator contract, not just one file.; No long-tail fallback exists for a request matching no enumerated capability×mode; adding one must not be conflated with the existing dispatch-execution fallbacks (native/inline when headless unavailable), a different layer (CONVENTIONS.md:140,514).; topologies.json rollout.route_compiler is 'report-only' with legacy_low_level_dispatch:true — a compiled-route path is scaffolded but not authoritative; assuming composition is already runtime-driven is wrong, dispatch is still hand-wired via dev-pipeline.md steps.

**open_questions**: Is capabilities/topologies.json intended to become the single compositional router (route_compiler promoted from report-only), with entry SKILLs collapsing into one? schema_version:2 + report-only compiler suggests migration underway but not owned by this surface.; Should 'TEAM = family label' bind to the existing metadata.fam/family field, and the current *-team native subagents be reclassified/renamed as ephemeral helpers? That is a cross-cutting rename beyond the skill layer.; Where should the long-tail fallback and ambiguity-arbitration judgment live — in the entry SKILL body, in WORKFLOW §0.2, or as a new dispatchable unit? Today ambiguity is human-gated only.; Modes appear enumerated in 3 generated places; is the intended end-state free unit composition (no fixed modes) or a curated enumeration? Needs the manifest-generator owner's intent.


## entry-layer-generation

**current_structure**: test A

**routing_today**: test B

**fits_direction**: test C

**breaks_or_gaps**: test D

**risks**: risk one; risk two


## skill-to-dispatch-chain

**key_files**: /home/Uihyeop/agent_setting/skills/autopilot-code/SKILL.md; /home/Uihyeop/agent_setting/skills/autopilot-code/references/owner-execution.md; /home/Uihyeop/agent_setting/skills/autopilot-code/references/dev-pipeline.md; /home/Uihyeop/agent_setting/capabilities/autopilot-code.md; /home/Uihyeop/agent_setting/capabilities/topologies.json; /home/Uihyeop/agent_setting/utilities/capability-route.py; /home/Uihyeop/agent_setting/tools/capability_topology.py; /home/Uihyeop/agent_setting/utilities/worker_bootstrap.py; /home/Uihyeop/agent_setting/utilities/stage-dispatch-fallback.py; /home/Uihyeop/agent_setting/utilities/dispatch-node.py; /home/Uihyeop/agent_setting/core/WORKFLOW.md

**current_structure**: Entry point is skills/autopilot-code/SKILL.md, a "compact pre-approval entry router" (frontmatter: group:entry, invocation_class:entry-router, modes dev/debug/audit; SKILL.md:6-13,18-27). It carries NO execution procedure. Pre-approval it uses only itself + harness-manifest.json + WORKFLOW section 0.2 to propose a route (SKILL.md:22-27). Post-approval the direct/quick session or the standard+ dispatch-depth-1 owner loads capabilities/autopilot-code.md then references/owner-execution.md, its "only post-approval reference edge" (SKILL.md:29-39). owner-execution.md sets the stage graph by intensity and points to dev-pipeline.md for the standard+ conductor loop.

Recipe registry = capabilities/topologies.json: an ENUMERATED recipes[] array, one entry per (capability, modes) pair. autopilot-code's entry (topologies.json:188-361) hard-codes topology_class, direct_predicates, promotion_signals, a quick block (one-shot-owner, one node), and a standard_plus block that literally lists nodes plan->execute->test->report with depends_on, write_scope, completion_gate, dispatch_depth:2, fallback_hops (224-347).

Compiler = utilities/capability-route.py. compile_route() (l.255) resolves the recipe by (capability,mode) via TOPO.resolve_recipe (l.263), validates caller predicates/signals against the recipe's known sets (264-270), derives effective intensity (275-276), emits an immutable hash-sealed route (l.342). For standard+ it does NOT assemble from a unit library; it deep-copies the recipe's pre-enumerated node array verbatim: nodes=json.loads(json.dumps(recipe["standard_plus"]["nodes"])) (l.304). direct -> 1 inline node (281-284); quick -> 1 one-shot node (294-297).

**routing_today**: Routing is decided at ENTRY and frozen before any worker spawns. (1) Case selection (which capability, which mode, intensity) is a human/main-agent judgment made pre-approval from SKILL.md + harness-manifest.json + WORKFLOW section 0.2 semantic primary routing (WORKFLOW.md:0.2 precedence list) and the section 0.4 five-field card. (2) That selection becomes concrete via capability-route.py compile, which resolve_recipe()'s the topologies.json recipe and pins the node graph into an immutable route file (route_hash/route_id sealed, l.342). (3) The standard+ dispatch-depth-1 owner is explicitly a "thin conductor" (dev-pipeline.md, "Standard+ Stage Dispatch"): it does NOT choose a recipe; it iterates the already-compiled nodes and dispatches each via utilities/dispatch-node.py --route --node --adapter start (dev-pipeline.md Step 1/3/4/5). (4) dispatch-node.py materializes one route node onto an adapter wrapper (dispatch-node.py header + select_checked_tuple); stage-dispatch-fallback.py executes the checked v3 ordered fallback chain for a node (same-harness-headless -> cross-harness-headless -> native-subagent -> inline; ORDER l.27, main loop l.682-869). Crucially the depth-2 workers do NO routing: worker-route-guard.py + the immutable route pin bound them to their assigned node/contract. So the only decision made at dispatch time is a TRANSPORT hop, never a recipe/case choice.

**fits_direction**: The 'routing at ENTRY, never at dispatch depth-2' tenet is already strongly realized. capability-route.py compiles an immutable, hash-sealed route BEFORE spawn; the depth-1 owner is a thin conductor that only dispatches pre-pinned nodes (dev-pipeline.md); depth-2 workers do zero routing (worker-route-guard.py, route immutability). The entry SKILL is literally labeled invocation_class:entry-router with no execution procedure (SKILL.md:9,18-21), matching 'entry skill is the router'. Units-as-dispatchable-atoms is present: each standard+ node dispatches a separate registered worker whose assigned_contract is resolved to code-plan/code-execute/code-test/code-report via worker_bootstrap.assigned_contract (completion_gate -> capabilities/<gate>.md, l.106-128), and those code-* capabilities are marked 'only when autopilot-code dispatches' (their SKILL descriptions) i.e. real work = dispatched unit. TEAM-as-family-label also fits: SKILL frontmatter fam:code is a label; native teams (디자인팀/연구팀/품질관리팀) appear only as optional native review helpers (dev-pipeline.md 'independent plan review'), not as routed units. Cross-harness registered tracked dispatch is the executor (stage-dispatch-fallback.py adapter wrappers, jobs.log registration).

**breaks_or_gaps**: (a) COMBINATORIAL, not compositional: topologies.json enumerates a full static node blob per (capability,mode). autopilot-lab setup vs eval are two separately enumerated recipes (topologies.json:689-816 vs 818-1035); the compiler copies recipe["standard_plus"]["nodes"] verbatim (capability-route.py:304) rather than composing a case from a unit library. Mode only SELECTS among pre-declared recipe variants; it never assembles units per-request. This is exactly the enumerated-blob shape the direction warns against. (b) The compiler is NOT the enforcing authority: topologies.json rollout.route_compiler = "report-only" and legacy_low_level_dispatch = true (topologies.json:46-49), and tools/capability_topology.py:198-199 HARD-FAILS if route_compiler is anything but "report-only" (test capability_topology.test.py:54-55 asserts "enforced" is rejected). So the compositional-route path is architecturally locked into report-only; legacy low-level dispatch remains the sanctioned de-facto path. (c) No long-tail fallback for un-enumerable requests: resolve_recipe on an unknown (capability,mode) just errors; fallback_hops is a TRANSPORT fallback (headless->native->inline), not a request-classification fallback. (d) The ambiguity-judgment step lives UPSTREAM of this chain (WORKFLOW 0.2 + human 0.4 card); the compiler demands explicit capability+mode+predicates+signals and does no classification itself (compile_route args, l.255-259).

**combinatorial_evidence**: topologies.json is the enumerated-blob structure: recipes[] has one full entry per (capability, modes) tuple, each embedding its own complete standard_plus.nodes graph (autopilot-code 188-361; autopilot-lab has TWO entries setup 689-816 and eval 818-1035; autopilot-spec 1533-1677). The 'units' code-plan/code-execute/code-test/code-report are reused only as assigned_contract labels resolved at bootstrap (worker_bootstrap.py:106-128, STAGE_NODE_CONTRACT l.26-35), NOT as composable inputs the compiler stitches together. compile_route() performs zero composition: standard+ = verbatim deep-copy of the recipe node array (capability-route.py:304); quick/direct = fixed single-node shapes hardcoded in the compiler (281-297). resolve_recipe keys strictly on (capability, capability_mode) (capability-route.py:263). There is no unit catalog, no per-case graph assembly, no recipe = compose(units). Adding a new case today means enumerating a new full recipe blob, not composing existing units. This is combinatorial, matching tension (a).

**cross_harness_diff**: This surface IS the harness-neutral core: capabilities/*.md, topologies.json, and utilities/*.py are portable, and stage-dispatch-fallback.py fans out to adapters/<harness>/bin/dispatch-headless.py for claude/codex/opencode (wrapper_command l.356-357, harness in {codex,claude,opencode}). The per-harness difference is only the wrapper/adapter projection and native-subagent surface naming (NATIVE_SURFACES codex-native-subagent vs claude-subagent, capability-route.py:44-47; stage-dispatch-fallback.py:834). Claude's SKILL.md and adapters/claude/skills/autopilot-code/SKILL.md are enforced byte-identical (autopilot-code.md:133-139); Codex/OpenCode consume the same portable spec via their own preflight capability-info projections (autopilot-code.md:134-135). Model/effort mapping and capacity cascade are per-adapter (adapters/<h>/config/models.conf, model-map.sh; stage-dispatch-fallback.py:278-342). So routing logic is identical cross-harness; only the terminal execution surface differs.

**risks**: Converging to a compositional compiler requires flipping rollout.route_compiler off 'report-only', but tools/capability_topology.py:198-199 hard-fails any other value and a test pins it (capability_topology.test.py:54-55); the compiler is deliberately non-authoritative today, so any redesign must plan the report-only -> enforced migration and retire legacy_low_level_dispatch.; The immutable hash-sealed route (route_hash over the full node blob, capability-route.py:342) is load-bearing for dispatch integrity (worker-route-guard, completion markers). A shift from enumerated blobs to composed graphs must preserve this per-route sealing or the depth-2 no-routing guarantee weakens.; code-plan/execute/test/report are only partially first-class units: they exist as capability contracts but the graph wiring (depends_on, write_scope, fallback_hops) is duplicated inside each recipe blob, so a compositional refactor risks contract/scope drift between the unit contract and its per-recipe node.; The long-tail/un-enumerable-request fallback the direction wants is entirely absent at this layer; an unknown (capability,mode) hard-errors in resolve_recipe with no graceful path.; Ambiguity judgment currently sits in WORKFLOW 0.2 + the human 0.4 card, outside the compiler; moving classification into the entry composition step must not silently bypass the human approval gate the compiler assumes as input.

**open_questions**: Where should compositional recipe assembly live if route_compiler stays architecturally report-only? Is the plan to introduce a separate enforcing composer, or to flip capability_topology.py:198-199?; Are the code-* stage capabilities intended to become the canonical unit catalog that recipes compose, or remain per-recipe-embedded nodes?; What is the intended de-facto authority today given legacy_low_level_dispatch:true, i.e. how often is the compiled route actually used vs raw wrapper dispatch in practice?; Does the converged 'entry composes units' allow per-request node graphs (dynamic depends_on), or only selection among fixed recipe variants as today?


## combinatorial-recipe-structure

**key_files**: /home/Uihyeop/agent_setting/capabilities/topologies.json; /home/Uihyeop/agent_setting/tools/capability_topology.py; /home/Uihyeop/agent_setting/harness-manifest.json; /home/Uihyeop/agent_setting/utilities/dispatch_contract.py

**current_structure**: capabilities/topologies.json (schema_version 2, 1692 lines) is a flat "recipes" array of 11 recipe objects (topologies.json:58-1678) covering 22 (capability,mode) keys. The top of file declares shared vocabularies referenced BY VALUE: intensities (3-10), execution_topologies (11-18), worker_kinds (19-25, 5 kinds), transports, tracking_values, guard_preconditions, inline_reasons, fallback_hops (1686-1691). Each recipe is a FULLY-INLINED static blob: direct_predicates, promotion_signals, a `quick` block, and a `standard_plus` block whose `nodes` array hand-authors every stage inline (id, kind, depends_on, role, inputs, outputs, write_scope, resource_class, completion_gate, dispatch_depth, fallback_hops), plus completion_gates, human_gates, resume_retry_boundaries. There is NO $ref / include / node-library / template mechanism anywhere — node bodies are re-authored per recipe. Multiple modes collapse onto ONE shared blob: autopilot-code covers dev/debug/audit with a single topology (189-361), autopilot-spec covers 6 modes with one (1533-1677) — modes are enumerated only for coverage, not compositional variation. 38 standard_plus nodes total (apply 3, code 4, design 4, draft 4, lab-setup 3, lab-eval 6, note 2, refine 2, research 4, ship 3, spec 3).

**routing_today**: Resolution is a direct (capability,mode)->blob table lookup: resolve_recipe() (capability_topology.py:207-211) linear-scans recipes for a capability match with mode in recipe['modes'] and returns the WHOLE static blob; it raises TopologyError on any unknown capability/mode (211). No composition happens at lookup — the topology is pre-authored, not assembled. Capability selection happens UPSTREAM (WORKFLOW §0.2 semantic route), so this file is only the map from an already-chosen (capability,mode) to a fixed topology. expected_recipe_keys()/recipe_keys() (36-44) bind coverage exactly to harness-manifest.json entry-group capabilities×modes (10 entry capabilities, 22 keys); validate_registry (200-202) fails on any missing or extra key, so the enumeration must be complete and closed.

**fits_direction**: The direction's "UNIT = composable dispatchable behavior atom" already has a concrete embodiment: each node object carries exactly a unit contract (kind, inputs, outputs, write_scope, completion_gate, dispatch_depth, fallback_hops) and every node is the dispatch grain (dispatch_depth 2 for stages, owner at depth 1). Shared vocabularies (worker_kinds, fallback_hops, roles, execution_topologies, resource classes) form a real compositional substrate referenced by value, so the atoms speak a common language. Mode-sharing (one blob covers 3-6 modes) already avoids per-mode combinatorial explosion at the leaf. A separate "sub" unit-skill inventory (13 skills) exists in the manifest, so raw material for a unit library is present.

**breaks_or_gaps**: The registry IS the combinatorial-blob structure the direction warns against: recipes are per-capability full static blobs, not compositions. No reuse-by-reference (no $ref/library) — units are duplicated inline (direct_predicates x11 identical, fallback_hops copy-pasted x36, 38 nodes -> 13 shapes). Node gates are mostly NOT tied to the sub-unit skills (30/38 recipe-local, evidence above), so the "unit" concept is not the composition primitive here — it is re-described per case. Routing is table lookup on an already-chosen capability, so no entry-level "compose units into a case" step lives in this artifact; the entry/case->composition the direction wants is entirely upstream and this file assumes the answer. No long-tail fallback: resolve_recipe raises on unknown (capability,mode) (capability_topology.py:211) and validate_registry demands EXACT coverage against the manifest (200-202, no missing/extra), so an un-enumerable request has no recipe and no default path. No ambiguity/judgment step for classification is represented here. Adding a new case today = author a whole new blob (new recipe object duplicating direct_predicates + quick + fully inlined nodes with re-specified fallback_hops/roles/scopes + gates + resume boundaries) AND add a manifest entry so coverage matches — combinatorial authoring, not composition.

**combinatorial_evidence**: Measured over the file: (1) direct_predicates is byte-identical across all 11 recipes (1 distinct block, count 11) — pure copy-paste. (2) the `quick` block minus write_scope is identical across all 11 (1 distinct). (3) fallback_hops has only 2 distinct arrays copy-pasted across 36 dispatchable nodes: ["same-harness-headless","cross-harness-headless","inline"] x21 and the +native-subagent variant x15. (4) The 38 nodes collapse to 13 shape signatures (kind+role+dispatch_depth+fallback_hops+resource_class); top shape "deep-reviewer review-worker" recurs 8x, "fast-writer pipeline-stage" 5x, "fast-implementer" 4x, "deep-orchestrator capability-owner" 4x, "deep-maker pipeline-stage" 4x. Same handful of unit shapes re-inlined many times rather than referenced. (5) DECISIVE: only 8 of 38 completion_gates match a "sub"-group unit-skill name in the manifest (the code-plan/execute/test/report and three design-* gates); the other 30 (draft-production, ship-setup, note-scan, refine-transaction, spec-atomic-transaction, lab-*, apply-*, research-*) are recipe-LOCAL gate strings with no corresponding sub-unit skill. The manifest DOES carry a units concept (13 "sub" skills: code-plan, code-execute, design-tokens, draft-strategy, …), but the recipe node graphs do NOT reference that inventory — they independently re-describe stages inline and only loosely align (design "build" node bundles design-tokens+design-components; design-init/draft-refine sub-skills appear in no node). Verdict: recipes are ENUMERATED full static blobs per capability, not COMPOSED from a reusable unit registry.

**cross_harness_diff**: This registry is explicitly PORTABLE/harness-neutral (capabilities/ is a portable source per CLAUDE.md hierarchy core->capabilities->{Claude,Codex,OpenCode}); it names no vendor. Harness specificity enters only via the shared dispatch_contract vocabularies it imports (capability_topology.py:13 EXECUTION_SURFACES/FALLBACK_HOPS/WRAPPER_TRANSPORTS) and the execution_surfaces list (topologies.json:1679-1685: registered-headless, codex-native-subagent, claude-subagent, claude-agent-team-teammate, inline) — the only place harness identities appear, and only as an enumeration the nodes' fallback_hops map onto. Unlike a per-harness adapter surface, this file is a single shared enumeration all harnesses resolve against; the combinatorial-vs-compositional question is decided once here for every harness.

**risks**: Refactoring toward a referenced unit library must preserve the validator's per-blob invariants (write-scope isolation for review/map workers at capability_topology.py:152-155, concurrent scope non-overlap 170-174, DAG acyclicity 156-163, max_dispatch_depth==actual 103-115) — defined over the fully-expanded graph, so a composition layer would need to expand-then-validate.; The exact-coverage gate (validate_registry:200-202) couples topologies.json to harness-manifest.json entry group; any long-tail/fallback recipe or compositional generation breaks the closed-set assumption and the coverage check.; registry_digest (capability_topology.py:32-33) canonicalizes and hashes the whole registry and is consumed by the gate_command (topologies.json:57) and route completion; moving from enumerated blobs to composed recipes changes the digest surface and any evidence bound to it.; Modes silently share one blob today; a naive per-(capability,mode) composition could re-expand into the mode-level explosion that the current mode-list sharing avoids.

**open_questions**: Where is the capability chosen and mode inferred before resolve_recipe is called (WORKFLOW §0.2 owner), and is that the intended home for the entry-level unit composition the direction wants?; Is the 30/38 gate-vs-sub-skill divergence intentional (recipe gates as a separate completion vocabulary) or drift a unit-referencing model would collapse?; Does any consumer (route compiler rollout=report-only, topologies.json:47) already attempt to compose nodes, or is resolve_recipe->whole-blob the only reader today?


## gap-vs-direction

**key_files**: core/WORKFLOW.md; capabilities/README.md; capabilities/topologies.json; core/OPERATIONS.md; core/CONVENTIONS.md; adapters/claude/agents/research-team.md; adapters/claude/agents/qa-team.md; adapters/claude/skills/code-plan/SKILL.md; adapters/claude/skills/code-execute/SKILL.md; adapters/claude/skills/code-test/SKILL.md; adapters/claude/skills/autopilot-code/references/dev-pipeline.md; adapters/claude/skills/autopilot-research/references/pipeline-search-analysis.md; adapters/claude/skills/autopilot-research/references/report-generation.md; adapters/claude/skills/autopilot-lab/references/eval-procedure.md; adapters/claude/skills/autopilot-design/references/owner-execution.md; adapters/claude/skills/design-review/SKILL.md; adapters/claude/skills/draft-refine/SKILL.md; adapters/claude/skills/analyze-project/references/mode-doc.md

**current_structure**: Two-tier skill layer. (1) 10 "entry"-group capabilities (autopilot-code/-research/-lab/-design/-draft/-refine/-spec/-note/-ship/-apply) plus 2 "pre" (analyze-project, analyze-user) and 2 "ops" (audit, post-it) are top-level router targets (capabilities/README.md group tally). (2) 13 "sub"-group capabilities (code-plan, code-execute, code-test, code-report, code-refine, design-review, design-components, draft-refine, etc.) are parent-invoked stages, explicitly "Not for top-level user requests or primary capability routing" (design-review/SKILL.md:4, code-plan description). (3) SEPARATELY, adapters/claude/agents/*-team.md holds 9 NATIVE Claude subagents — research-team(연구팀), qa-team(품질관리팀), plan-team, dev-team, design-team, material-team, editorial-team, codex-review-team, memory-scout — each a real runtime agent with frontmatter name/tools/model (research-team.md:1-11, qa-team.md:1-10). The behavior atoms the direction calls "units" (code-review, plan-review, test, research-survey, fact-check) exist ONLY as MODES of these native team subagents (qa-team.md metadata.modes:[code-review,plan-review,test,ml-debug,data-curate,security-review]), not as independently dispatchable units.

**routing_today**: Two routing layers exist. ENTRY routing: the MAIN agent (there is no entry SKILL) reads WORKFLOW §0.2 semantic precedence prose (WORKFLOW.md:96-130) + §2 work-nature map (WORKFLOW.md:261-271), selects one primary capability, proposes it via the §0.4 five-field card, user confirms once (WORKFLOW.md:302-305); then "internal routing is automatic." DEPTH routing: at standard+ each durable stage is dispatched as an independent depth-2 headless session, but "the named team roles run inside that session" (WORKFLOW.md:311) and inside that stage the native team subagent runs its OWN sub-router — research-team.md self-describes as "research-team router... dispatches four research responsibilities as modes", and both research-team.md and qa-team.md carry a "Team Member Selection" mode table. So the composable-atom (unit-mode) decision is made at depth-2 inside a native subagent, not at entry.

**fits_direction**: Scaffolding for the direction already exists. (1) Entry-as-router surface: manifest defines an "entry-router" concept with discovery/execution as separate surfaces (capabilities/README.md:26-31); 10 entry-group capabilities are the confirmed router candidate set, and §0.4 is a single top-level handshake. (2) Unit-as-dispatch-atom is PARTIALLY realized for STAGES: the 13 sub-capabilities are dispatched as depth-2 headless sessions with strict file-only handoff (WORKFLOW.md:311; topologies.json staged recipes; pipeline-search-analysis.md:3). (3) native-as-helper-only is already the stated intent: OPERATIONS §5.10 restricts "Light team delegation" to "small, fast iterations" quick/micro only (OPERATIONS.md:138), defines the four delegation-surface nouns distinctly (OPERATIONS.md:104-113), and keeps native-subagent vs registered headless-dispatch as separate surfaces (CONVENTIONS.md:140). (4) routing-at-entry for capability choice is real via §0.2 semantic precedence.

**breaks_or_gaps**: FIVE concrete breaks vs the direction. (A) TEAM-AS-LABEL-NOT-AGENT is violated: teams are real runtime native subagents, not family labels — adapters/claude/agents/research-team.md:1-11 and qa-team.md:1-10 declare name/tools/model/color and are invoked as agents. (B) SKILLS INVOKE NATIVE TEAM SUBAGENTS INSTEAD OF DISPATCHING UNITS — direct evidence in stage skills and references: code-plan/SKILL.md:36 (plan-team),:68-69 (qa-team plan-review); code-execute/SKILL.md:99 (qa-team); code-test/SKILL.md:26 (qa-team test); autopilot-code/references/dev-pipeline.md:79 ("invoke native 자료팀 inside code-execute or code-report"); autopilot-research/references/pipeline-search-analysis.md:71,133,160,185,203,216,226,242 and report-generation.md:15 (Agent(subagent_type="연구팀"/"자료팀")); autopilot-lab/references/eval-procedure.md:51,57,61 (Agent(subagent_type="테스트팀"/"자료팀"/"연구팀")); autopilot-design/references/owner-execution.md:30-32,99,101,102 (design-team/material-team); design-review/SKILL.md:35,92 (design-team); draft-refine/SKILL.md:37,46,65,90,136-137 (research-team); analyze-project/references/owner-execution.md:81,83,84,85 + mode-doc.md:27 + mode-code.md:192 (research-team, qa-team). So the real behavior atom (a "unit") is a native-subagent mode, not a dispatched unit. (C) ROUTING-AT-DEPTH-2 is violated: mode/unit selection happens inside depth-2 native team routers (research-team.md + qa-team.md "Team Member Selection" tables; team self-labels as "router"). (D) NO LONG-TAIL FALLBACK for un-enumerable requests: §2 work-nature map (WORKFLOW.md:261-269) is a fixed 6-row enumeration; the only escape hatch is a DOWNGRADE to inline direct editing ("One-line edits... may bypass autopilot", WORKFLOW.md:271), not a catch-all capability; capabilities/README.md has no general/misc/other capability and no long-tail route. (E) AMBIGUOUS-JUDGMENT IS PARTLY PUSHED DOWN: entry ambiguity is handled at entry (WORKFLOW §0.2 precedence + one-round ask at §0.4/CONVENTIONS.md:482), but classification judgment is ALSO DELEGATED DOWNWARD to a depth-2 native subagent — analyze-project/references/mode-doc.md:23 "If classification remains ambiguous, delegate the judgment to the research-team" — which directly contradicts routing/judgment-at-entry.

**combinatorial_evidence**: topologies.json is compositional at the STAGE level but there is no shared UNIT library that entry cases compose from. Each recipe is keyed by a single capability and enumerates its own full node graph (e.g. autopilot-apply staged nodes; every entry capability carries its own quick + standard_plus block). Reuse across capabilities happens only for the code-* stage skills; the finer behavior atoms (code-review, plan-review, test, research-survey, fact-check, ml-debug, security-review) are NOT unit rows — they are enumerated as modes inside native team agent files (qa-team.md and research-team.md metadata.modes + "Team Member Selection" tables). Net: two enumerated routers stacked — an entry capability list (WORKFLOW §2/§0.2) plus a per-team mode list at depth-2 — rather than one entry-level composition over a flat unit library. No topologies.json entry references a "unit" primitive or a fallback/long-tail recipe.

**cross_harness_diff**: This surface is Claude-adapter-specific. The native "team" subagents live only under adapters/claude/agents/*.md; capabilities/ (portable) has NO team concept — it defines capabilities + portable ROLE families (CONVENTIONS §2 model roles, e.g. deep reviewer/maker/orchestrator, CONVENTIONS.md:93-121). So the portable layer already treats "team"-like things closer to labels/roles, while the Claude adapter reifies them as runtime agents. OPERATIONS §5.10:104-113 enumerates the four surfaces across harnesses (Codex native subagent, Claude subagent, Claude agent-team teammate session, registered headless worker) — the direction's "native = helper only" maps to the first two, "dispatch" to the last; the gap is that Claude skills currently route real tracked work through the Claude-subagent (team) surface rather than the registered-dispatch surface. Codex/OpenCode realizations are generated projections (capabilities/README.md:10-11) and were not inspected here.

**risks**: Reclassifying teams from native subagents to pure labels touches every stage skill and reference that calls Agent(subagent_type=...) — at least 12 files / 40+ call sites enumerated above; a mechanical rename risks silently dropping the depth-2 mode-router logic that currently lives in the team agent files.; The behavior atoms (code-review, plan-review, test, research-survey, fact-check, ml-debug, security-review) have no existing unit/capability home; promoting them to dispatchable units requires new sub-capability contracts + topologies.json unit rows that do not exist today.; Removing depth-2 team routing (research-team.md/qa-team.md 'Team Member Selection') without relocating that judgment to entry would leave ambiguous mode-selection homeless — see the explicit downward delegation at analyze-project mode-doc.md:23.; No long-tail fallback capability exists; adding one interacts with the hard artifact-order/no-code-without-spec gates (WORKFLOW.md §0.1) and the fixed §2 map, so a catch-all must not become a spec/gate bypass.; OPERATIONS §5.10 already forbids depth 3+ (pipeline-search-analysis.md:3); if units become independently dispatched from inside a depth-2 stage that currently only calls in-session native teams, the depth budget must be re-derived or it will violate the depth-3 ban.

**open_questions**: Should the 9 native *-team agents be deleted and their modes re-homed as dispatchable sub-capabilities (units), or kept as thin ephemeral helpers while units become new topologies.json rows?; Where should the depth-2 mode-selection judgment (currently in team 'Team Member Selection' tables) move — into the entry skill's case->composition, or into each stage skill's inline logic?; Does any request today legitimately need a long-tail/catch-all capability, or is 'downgrade to inline direct editing' (WORKFLOW.md:271) considered sufficient for un-enumerable work?; Is 'entry-router' in capabilities/README.md:26-31 intended to become a single composing entry skill, or does the direction keep 10 separate entry capabilities with composition inside each?
