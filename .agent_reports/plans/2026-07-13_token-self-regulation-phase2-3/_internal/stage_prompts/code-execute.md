# Stage: code-execute — Token Self-Regulation Phase 2/3

You are the depth-2 `code-execute` stage worker and the only stage allowed to mutate source. Do not dispatch any child/headless job (depth 3 is forbidden). Work only in `/home/Uihyeop/agent_setting-wt/token-self-regulation-phase2-3`; do not modify the main checkout and do not commit/push/merge/clean worktrees.

Read and execute the durable file-only handoff at `.agent_reports/plans/2026-07-13_token-self-regulation-phase2-3/{plan.md,checklist.md}` plus the actual component SoT `.agent_reports/spec/token-self-regulation/{prd.md,experiment_contract.md,pipeline_state.yaml}`. Follow the Codex bootstrap, `code-execute` Skill, `dev/refactor` mode, core-first/read/write gates, and the exact file/class ownership in the plan. The spec-significance verdict is `within-spec`.

Implement v2 exactly:

- Phase 2 bounded content-free sha256 session accounting, exact invocation/zero/emission/reason accounting, exact inserted directive UTF-8 bytes, monotonic exact-session token delta with decrease/unavailable counts, no estimate absent tokenizer provenance, atomic bounded lock/store <=8 KiB/file <=256 files <=2 MiB oldest-first prune, always fail-open. Preserve Phase 1 production output byte-for-byte and all zero paths. L2 accounting diagnostics only in kv/json; never persist content or label metrics savings/billing/cost/ROI.
- Phase 3 pure deterministic production-disabled `offline-forecast-v1`, frozen decision features/directive IDs, manifest/replay fixtures, episode duplicate suppression, unknown => no early emission. Add isolated control/static/dynamic schema/evaluator with the exact exclusions and gates from the contract: n>=30/per-stratum>=10, required+safety 100%, no hard regression, both quality LCB >= -0.02, both observed-delta one-sided paired bootstrap LCB >0 with 10,000 seed 20260713, exact bytes/emissions, max verdict eligible_for_user_review, adoption pending.
- No production dynamic import/activation/config mutation; no pruning/RL/online fitting/model-effort/intensity/dispatch/QA/guard changes. Reuse canonical Fleet modules and mirror conventions. Apply portable invariant core-first where plan requires it, preserve required Claude mirrors, Codex selective projection, and explicit OpenCode defer. Never edit runtime-owned config.toml.

Own/write only source and implementation-class artifacts listed by the plan, including `.agent_reports/plans/2026-07-13_token-self-regulation-phase2-3/{checklist.md,dev_logs/**,_internal/dev_reviews/**}` and plan status. Do not write `test_logs`, `_internal/test_reviews`, `pipeline_summary.md`, or final report. Run only focused implementation sanity checks; final verification belongs to code-test. Record exact changed files, decisions, any apply_patch hook fallback, and remaining verification obligations in `dev_logs/implementation.md`. End with a concise `READY_FOR_CODE_TEST` or failure verdict.
