You are a Claude headless depth-2 worker (worker_role=code-test, owner=autopilot-code, parent=fleet-ui-v2). code-test ROUND 2 — full regression + live re-verification after the execute-r2 remediation. Your turn ends when the process exits — do ALL work in this turn.

Sub-skill: code-test (품질관리팀 test 모드).

CONTEXT — this is round 2 of testing for the fleet UI v2 cycle (PRD v2 §4.5·§4.6). Round 1 found defect D1 (raw code-* stage-prefix leak) + D2 (test gap); execute-r2 fixed both and it is now COMMITTED (branch fleet-ui-v2, commit 381cc92). Your job: verify the fixed tree end-to-end, confirm D1 does not recur, and confirm no render regression.

INPUT ARTIFACTS (read directly):
- Round-1 test report: /home/Uihyeop/agent_setting-wt/fleet-ui-v2/.agent_reports/plans/2026-07-10_fleet-ui-v2/test_logs/test_round1.md
- execute-r2 remediation log: /home/Uihyeop/agent_setting-wt/fleet-ui-v2/.agent_reports/plans/2026-07-10_fleet-ui-v2/dev_logs/execute_round2.md
- Plan (SD-F1~F4, F-9~F-13 acceptance): /home/Uihyeop/agent_setting-wt/fleet-ui-v2/.agent_reports/plans/2026-07-10_fleet-ui-v2/plan/plan.md

VERIFICATION ITEMS (graduated — run every one, record command + result):
(a) FULL tests green — `cd tools && python3 -m unittest discover -s fleet/tests -v` (or at minimum `python3 -m unittest fleet.tests.test_dispatch`). Report the exact ran-count and OK/FAIL. All must pass.
(b) LIVE fixture re-verify — `python3 tools/fleet/fleet.py --once` and `python3 tools/fleet/fleet.py --json --once`. Confirm THIS cycle's depth-2 stage rows render with the humanized stage label (exec/test/plan/report), NOT a raw `code-*` prefix. Paste the proving row line(s). If no depth-2 stage row is live at capture time, use `--demo --once` and/or a synthetic jobs.log fixture row (key=code-test, depth=2) to prove the render path, and say so.
(c) Conductor row aggregation — confirm the depth-1 conductor row still renders `code: plan › exec › test` breadcrumb (no regression).
(d) D1 non-recurrence — grep the live `--once` and `--demo --once` output for `code-(plan|execute|test|report):` ; must be ZERO matches. State the grep + result.
(e) Render regression — run any existing render/demo tests and eyeball the wide + narrow (2-line card) layouts; confirm no breakage vs round 1 (F-9~F-13 readability still intact: legend, column headers, model cell, breadcrumb).
Also re-confirm the D2 guard test fails-before/passes-after is still valid (spot check: the strengthened assertNotIn(worker_role+":") is present in test_dispatch.py).

OUTPUT CONTRACT:
- Write a round-2 test report: /home/Uihyeop/agent_setting-wt/fleet-ui-v2/.agent_reports/plans/2026-07-10_fleet-ui-v2/test_logs/test_round2.md — per-item (a)-(e) command + result, PASS/FAIL verdict, any new defect (Dn) with file:line + severity, and an overall verdict line. If a new defect is found, mark it clearly so the conductor can decide on another remediation round.
- Commit on branch fleet-ui-v2 (test_logs + this prompt). Do NOT merge/push/clean worktree.

Return a concise Korean summary: overall verdict (all-green or defects), test ran-count, the proving breadcrumb evidence for (b)/(d), and the test_round2.md path.
