# Audit Report — pocock-parity-efficiency

- **Date**: 2026-07-15 12:26
- **Type**: plans
- **Scope**: auto (status done + 검증 로그 존재)
- **Aspects checked**: 테스트 결과, 코드 리뷰, 의미-결정론 일관성
- **P1 baseline**: _skipped: plans 유형은 마이너 로그 규약이 없어 P2만 수행_

## Summary

| Aspect | 🔴 Critical | 🟡 Warning | 🟢 OK |
|---|---|---|---|
| 테스트 결과 | 0 | 1 | 12 |
| 코드 리뷰 | 0 | 1 | 1 |
| 의미-결정론 일관성 | 0 | 0 | 5 |

**Total**: 🔴 0 / 🟡 2 / 🟢 18

## Perspective 2 — Universal Principles

### Aspect: 테스트 결과

verdict(`test_logs/00_VERDICT.md`)가 주장한 검증을 현재 main 트리에서 전부 재실행해 재현했다.

| Verdict 주장 | 재실행 결과 |
|---|---|
| `hooks/portable-guards.test.sh` PASS=355 FAIL=0 | ✅ 재현 (정화된 환경 기준, 아래 🟡 참조) |
| `tools/adaptation-guard.test.sh` 7 negative + 상태 복원 | ✅ PASS, 실행 전후 `git status`/`git diff` 해시 동일 확인 |
| `tools/generated-projections.test.sh` | ✅ PASS (rc=0) |
| `tools/routing-contract.test.sh` | ✅ all checks passed |
| `tools/skill-conformance/check.sh` 4 tree + 26 classification | ✅ PASS |
| `tools/install/profile-activation.test.sh` | ✅ PASS (rc=0, verdict 이후 커밋 반영분 포함) |
| `tools/install/runtime-activation.test.sh` | ✅ PASS (rc=0) |
| `tools/context-footprint.py --strict` | ✅ status=ok, warning 0 |
| 공통 runtime doctor ×3 (builder/14/7/26, duplicate 0) | ✅ ok=true — 다만 freshness는 `session-reload-needed` (verdict 이후 커밋 3건 때문이며, 새 세션에서 해소되는 정상 상태) |
| Codex `preflight.sh doctor --runtime` | ✅ status=ok, `runtime-projection:ok` |
| OpenCode `preflight.sh headless --check <worktree>` | ✅ check=ok |
| `tools/generate.py --check` | ✅ manifest up-to-date, 10 projection group |

정적 footprint 수치도 실측과 일치했다. bootstrap은 Claude 4,632 / Codex 7,646 / OpenCode 6,045 = 18,323 B로 보고서 표와 같았고, 설치본(`~/.claude/CLAUDE.md`, `~/.codex/AGENTS.md`, `~/.config/opencode/AGENTS.md`)도 저장소와 바이트 단위로 동일해 3개 하네스의 동등성을 확인했다.

#### 🟡 portable-guards 스위트가 활성 세션 환경변수로부터 격리되지 않음

- **File**: `hooks/portable-guards.test.sh:1` (러너 전문), `adapters/codex/bin/dispatch-headless.py:93`
- **Severity**: 🟡
- **Detail**: 활성 Claude 세션 안에서 실행하면 PASS=344 FAIL=11로 위양성 실패가 발생한다. 원인은 두 가지다. ① `CLAUDE_CODE_SESSION_ID` 상속 → dispatch 래퍼가 registry 행에 `parent_sid=`를 삽입해 정확 문자열 매칭 4건이 실패한다. ② `CLAUDE_CODE_CHILD_SESSION=1` 상속 → 훅의 `is_worker_session()`이 참이 되어 session-start 주입, session-end distill, token-budget 계측 테스트 7건이 조용히 실패한다. 두 변수를 unset하면 355/0이 재현된다. 구현 회귀가 아니라 테스트 위생 문제다.
- **Introduced**: _기존 문제 (dispatch/hook 테스트 도입 시점부터). metrics.md에 기록된 fixture-isolation 후속 과제와 같은 계열_
- **Suggested fix**: 러너 앞부분에서 세션 정체성 변수(`CLAUDE_CODE_SESSION_ID`, `CLAUDE_CODE_CHILD_SESSION`, `CLAUDECODE`, `CODEX_THREAD_ID`, `CODEX_SESSION_ID`, `AGENT_DISPATCH_*` 등)를 unset하거나 정규화한다 — `context-footprint.py`가 hook 샘플에서 `MEM_STORE`/`XDG_STATE_HOME`을 격리하는 것과 동일한 원칙이다.

### Aspect: 코드 리뷰

#### 🟡 아티팩트에 독립 dev review 기록이 없음

- **File**: `.agent_reports/plans/2026-07-15_pocock-parity-efficiency/_internal/`
- **Severity**: 🟡
- **Detail**: 인접 plan 아티팩트(`2026-07-15_main-worker-bootstrap`의 `dev_reviews/`, `2026-07-14_canonical-artifact-worktree-cleanup`의 `plan_reviews/`)와 달리 이 아티팩트의 `_internal/`에는 `metrics.md`만 있다. plan review는 spec 레벨(`spec/skill-design-refactor/_internal/plan_review_r1.md`)에 있으나, 독립 모델 QA나 코드 리뷰 기록은 없다. verdict가 "독립 모델 QA가 아니라 결정론적 source/runtime 검증"이라고 스스로 밝혔으므로, 숨긴 것이 아니라 공개된 한계다.
- **Introduced**: _이번 사이클 (headless dispatch 실패 후 inline fallback 수행의 부산물)_
- **Suggested fix**: 다음 사이클에서 이 diff에 대한 독립 code-review를 뒤이어 실행하거나, inline fallback 시에도 리뷰 기록을 `_internal/dev_reviews/`에 남기도록 규약으로 명시한다.

### Aspect: 의미-결정론 일관성

spec `spec/skill-design-refactor/prd.md` v3 (SD-11~SD-16) ↔ 구현 대조 — 불일치 없음:

- SD-11 (의미 정본 = core/capabilities/roles, 3개 어댑터가 동급 sibling) ↔ `core/ADAPTATION.md:37` sibling 계약 명문화. ✅
- SD-13 (세 표면 모두 검증돼야 GREEN, 아니면 PARTIAL) ↔ plan.md Plan check 규칙 + verdict의 "다른 adapter 성공으로 대체 없음" 진술, guard 반영. ✅
- SD-16 (>5% footprint 회귀 실패) ↔ `tools/context-footprint.py:23` `MAX_BASELINE_GROWTH = 1.05` + 저장 baseline 대조. ✅
- 예산 계약(16,384 B bootstrap / 7,000자 metadata / 240 B directive / paired n≥30) ↔ `core/ADAPTATION.md:216-232`와 `context-footprint.py` 상수 일치. ✅
- 절감률 주장 게이트: 어떤 산출물도 정적 byte 감소를 token·비용 절감으로 환산하지 않았다(prd.md:59 요구와 일치). spec이 이 구간을 결정론 규칙 구간(§0.5)으로 지정했으므로, 의미를 토큰 절감으로 축소하는 문제는 없다. ✅

## Verdict

- **Status**: 🟡 warnings only — 구현·검증·수치 주장은 전부 재현되며, 경고 2건은 테스트 위생과 리뷰 기록 규약에 관한 것
- **Recommended next action**: `hooks/portable-guards.test.sh` 앞부분에 세션 환경변수 정화 추가 (활성 세션 내 검증 시 위양성 11건 제거)
- **Auto-fix dispatched**: yes (→ autopilot-code quick, 사용자 승인 후 2026-07-15 12:40) — `hooks/portable-guards.test.sh` 러너에 D-42 세션 정체성·worker 마커 unset 추가, 활성 세션 환경 그대로 재실행해 PASS=355 FAIL=0 확인. 리뷰 기록 규약(🟡 2번)은 후속 사이클 과제로 남김.

---

> Generated by `/audit`. The audit report itself applies no artifact edits.
