# Plan refinement

The independent plan check initially failed. The owner amended the plan before
source execution to make the watchdog a bounded synchronous
`stage-dispatch-fallback --start` responsibility, persist exactly-one capacity
retry authority in the canonical registry, require adapter-paired alternative
model settings with concrete allowlist resolution, define a two-window exact
stale-row predicate, remove a nonexistent Claude preflight projection, and
reserve live nested smoke plus the final commit for the depth-1 owner. Exact
canonical artifact names and `qa: thorough` are now explicit.

The second review clarified that observation continues across deterministic
progress until an explicit maximum-window budget, so a later pair of quiet
windows is still caught. It also pins capacity allowlisting to checked adapter
role/model maps and tracked profiles, permits repeated heartbeat phases only
with a changed evidence digest, and names the exact worker-prompt and liveness
consumers of the shared classifier.

The third review separated `dead-stale-terminal` from exact PID death: it now
requires a valid SD-56 completion marker and existing evidence artifact newer
than an otherwise unterminated registry row; exact-dead rows stay in their own
classification path.
