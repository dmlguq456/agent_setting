# Plan check

- Requirements coverage: language policy, runtime realization, recall logic,
  spec synchronization, and regression coverage are included.
- Over-scoping guard: installer and root README changes are explicitly excluded.
- Executable verification: focused memory tests plus projection and adaptation
  checks are named.
- Main risk: generated copies can drift from portable sources; generators and
  byte-equivalence checks are required before completion.
