No asynchronous Monitor/wakeup/scheduling waits. Poll every registered child synchronously in this turn with `utilities/dispatch-wait.sh`, inspect its typed handoff, publish/repair the exact hash-bound marker when a namespace-local terminal heartbeat requires reconciliation, and harvest before advancing. Never pass this owner prompt to a depth-2 child.

Own the approved `autopilot-code debug / standard` cycle for the Fleet memory-sync attribution bug. Immutable route: `/home/Uihyeop/agent_setting/.agent_reports/plans/2026-07-22_fleet-memory-sync-events/_internal/code-route.json` (`rt-d7392fcfbc9ce241`). Worktree: `/home/Uihyeop/agent_setting-wt/fleet-memory-sync-events` at sealed `e8938809d87e54474f5e7242a2552598c2636a0a`. Canonical cycle root: `/home/Uihyeop/agent_setting/.agent_reports/plans/2026-07-22_fleet-memory-sync-events`.

Read `capabilities/autopilot-code.md` completely. Run exactly the sealed nodes sequentially with these slugs, modes, model roles, and prompt files:

1. `plan`: `fleet-memory-sync-code-plan`, `plan/plan-author`, `deep maker`, `_internal/code-plan-prompt.md`.
2. `plan-check`: `fleet-memory-sync-code-plan-check`, `qa/plan-review`, `fast reviewer`, `_internal/code-plan-check-prompt.md`.
3. `execute`: `fleet-memory-sync-code-execute`, `dev/backend`, `fast implementer`, `_internal/code-execute-prompt.md`.
4. `impl-review`: `fleet-memory-sync-code-review`, `qa/code-review`, `fast reviewer`, `_internal/code-impl-review-prompt.md`.
5. `test`: `fleet-memory-sync-code-test`, `qa/test`, `fast reviewer`, `_internal/code-test-prompt.md`.
6. `report`: `fleet-memory-sync-code-report`, `editorial/report`, `fast writer`, `_internal/code-report-prompt.md`.

Use `preflight.sh dispatch-chain --route ... --node ... --parent fleet-memory-sync-code-owner --prompt-file ... --start`; no other node, child, or dispatch depth 3. A semantic FAIL is not a PASS: for a plan-check finding, refine the plan through a bounded retry of the plan node and recheck; for implementation/test findings, redispatch the mutation `execute` node in-place with a new slug and a bounded fix prompt, then rerun downstream review/test. Keep all retry evidence and never overwrite a successful immutable attempt linkage.

The spec prerequisite is complete at `/home/Uihyeop/agent_setting/.agent_reports/plans/2026-07-22_fleet-memory-sync-events/_internal/spec-owner-report.md`. The final implementation must use literal `sync` actor, create-only journaling, logical source cwd with explicit omission, prospective-only/no-backfill behavior, and a deterministic Fleet `agent-note` grouping proof. Preserve unrelated user work and keep the collector unchanged unless evidence requires it.

Codex linked-worktree mutation stages and this owner cannot commit Git metadata. Do not widen the sandbox or claim a commit. After every node PASS and the final report marker, write `_internal/code-owner-report.md` summarizing attempt IDs, fallback/retry trace, diff ownership, and gates; close/harvest the exact owner row; return the kernel's exact three-line handoff. Main will inspect, commit, integrate, push, and clean up only after independent PASS.
