# Pipeline Summary

Status: done — PASS

The v4 entry-route handshake is implemented portable-first and projected to
Claude, Codex, and OpenCode. Material work now receives one agent-filled
five-field proposal before execution; read-only orientation is exempt, direct
is explicit, and approved internal stages do not ask again.

Routing metadata is manifest-owned and the invocation registry is generated.
Only 13 entry-router capabilities are primary candidates; 13 parent stages and
the post-it model-support surface remain model-visible with explicit top-level
exclusions. Generated Codex/OpenCode entry bodies are compact and defer full
contract reading to the approved owner/worker boundary.

All targeted projection, conformance, footprint, adapter-boundary, and installed
runtime checks passed. The measured metadata increase is recorded as the cost
of better first-rung routing; only static main-context isolation is claimed.
