### Stage D: Report

Write `{artifact_dir}/_internal/audit/audit_{YYYY-MM-DDTHHMM}.md`:

~~~markdown
# Audit Report — {artifact name}

- **Date**: {YYYY-MM-DD HH:MM}
- **Type**: {plans | research | documents}
- **Scope**: {flag value or "all"}
- **Aspects checked**: {comma-separated}
- **P1 baseline**: v{N} snapshot ({YYYY-MM-DD}), {count} accumulated minor entries | _skipped: no snapshot or minor log_

## Summary

| Aspect | 🔴 Critical | 🟡 Warning | 🟢 OK |
|---|---|---|---|
| {aspect 1} | {count} | {count} | {count} |

**Total**: 🔴 {N} / 🟡 {M} / 🟢 {K}

## Perspective 1 — Accumulated Minor Drift vs v{N}

> Documents and research only; skip for plans.

### 1.1 Accumulated Minor Entries

| Version | Time | Trigger Summary | Audit Flag | Files |
|---|---|---|---|---|
| v{N}_M | ... | ... | facts/style/... | {count} |

### 1.2 Diff Summary

- **Lines added/removed**: +{A} / -{B}, excluding `_internal/` and `pipeline_summary.md`
- **Files modified**: {relative paths}
- **Hot spots**: {files with at least 20 changed lines}

### 1.3 Correlation with Perspective 2

| P2 finding | Matching minor entry | Introduced |
|---|---|---|
| {aspect:🔴 title} | v{N}_M ({YYYY-MM-DD}) | recent; prioritize fix |
| {aspect:🟡 title} | none | pre-existing residue |

Match when the P2 `file:line` belongs to a minor entry's `Files touched`.

## Perspective 2 — Universal Principles

### Aspect: {name}

#### 🔴 {issue title}
- **File**: `{relative path}:{line}`
- **Severity**: 🔴
- **Detail**: {1–3 lines}
- **Introduced**: v{N}_M ({YYYY-MM-DD}) | _pre-existing or untraceable_
- **Suggested fix**: {one line or null}

## Verdict

- **Status**: 🔴 requires attention | 🟡 warnings only | 🟢 clean
- **Recommended next action**: {one line}
- **Baseline reset recommendation**: {when at least five minor entries exist and P2 is clean or fixed, recommend one major refine, a v{N+1} snapshot, and minor-log cleanup; otherwise omit}

---

> Generated by `/audit`. The audit report itself applies no artifact edits.
~~~

#### Stage D.5: Editorial Polish

Before chat output, invoke the `editorial/polish` unit with in-place polish (inline at direct intensity, or as a one-node composed route):

```text
Dispatch unit editorial/polish:
  prompt: `polish {audit_log_path}
This audit report is user-facing. Apply the editorial/polish pass for natural phrasing, notation consistency, and readable cadence in the selected language.
Preserve severity, aspect, file:line, issue identity, and suggested-fix meaning. Edit wording only.`
```

Then print at most eight localized lines containing completion, artifact name and type, aspects, totals, report path, verdict, and—when 🔴 exists—the recommended corrective invocation.

### Stage E: Corrective Chain — default

After writing and reporting the audit, dispatch correction unless `--report-only` is set.

1. Skip when `--report-only` is set or the audit has no 🔴 or 🟡 findings. Print `✓ Audit clean — no corrective dispatch needed.` for a clean result.
2. Build one prompt from all 🔴 and significant 🟡 findings:

   ```text
   Automatically correct audit findings:
   - {issue 1} → {suggested fix}
   - {issue 2} → {suggested fix}
   Source audit report: {audit log path}
   ```

3. Dispatch plans to `autopilot-code --mode dev`; dispatch research and documents to `autopilot-refine`.
4. Alert the user before dispatch with the selected Skill and finding counts. The user may interrupt before the next Skill runs.
5. Append `**Auto-fix dispatched**: yes (→ {skill}) | no (--report-only or clean)` to `## Verdict`.

The default chain exists because reports were ignored during the motivating incident, allowing five factual drifts to persist across more than twenty refine cycles. `--report-only` preserves inspection-only and handoff-review use cases.
