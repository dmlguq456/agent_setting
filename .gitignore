# Credentials & secrets
.credentials.json

# Session data (per-machine, ephemeral)
history.jsonl
sessions/
session-env/
shell-snapshots/
paste-cache/
file-history/
ide/
debug/
cache/
downloads/
backups/
telemetry/
todos/
tasks/
plans/
plugins/

# Project-scoped memory (per-project, not shared)
projects/

# NFS temp files
.nfs*

# MCP runtime cache (per-machine, ephemeral)
mcp-needs-auth-cache.json

# Cleanup timestamp (per-machine, ephemeral)
.last-cleanup

# analyze-user 내부 작업 trace 자체 (draft / consensus / qa / runs / versions / converted 자료)
# — 본인이 운영하는 process artifact, 다음 cycle 자체 자체 갱신 / 재생성 자체 자체
user_profile/_internal/
