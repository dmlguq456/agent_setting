# Credentials & secrets
.credentials.json

# Session data (per-machine, ephemeral)
history.jsonl
sessions/
session-env/
shell-snapshots/
paste-cache/
file-history/
ide/
debug/
cache/
downloads/
backups/
telemetry/
todos/
tasks/
plans/
plugins/

# Project-scoped memory (per-project, not shared)
projects/

# NFS temp files
.nfs*

# MCP runtime cache (per-machine, ephemeral)
mcp-needs-auth-cache.json

# Cleanup timestamp (per-machine, ephemeral)
.last-cleanup

# analyze-user 변환 자료 (대용량 PDF / PNG, 재생성 가능)
user_profile/_internal/converted_pdfs/
user_profile/_internal/converted_pngs/
