---
description: 프로젝트 pipeline 순서 강제(research→spec→plan→code) opt-in 토글
allowed-tools: Bash(bash:*)
---
!`bash "$HOME/.claude/pipeline-toggle.sh"`

위 결과대로 현재 프로젝트의 pipeline 순서 강제(`.claude_reports/.pipeline` 마커)를 토글했습니다. 켜지면 📌tracked 일 때 코드 편집이 spec+plan 전제를 요구합니다. 한 줄로만 결과를 확인해 주세요.
